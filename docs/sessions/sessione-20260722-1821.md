# Checkpoint Sessione — 2026-07-22 18:21

## progetto: ai-router-switch

---

## ✅ Completati

### Fix sistema retry 429/5xx (mix-am/mix-ag)

- Helper centralizzato `anthropic_call_with_retry()` in `pipeline_common.py`:
  - Exponential backoff certificato SDK (`max_retries=3`, jitter)
  - Retry su 429 + 5xx transienti
  - Parsing `retry-after` header per rate-limit reali
- `ai-router-proxy.py` (path puro): delega all'helper (wrapper `forward_anthropic_with_retry`)
- `_call_full()`: param `retry_transient=True` → ritenta 429/5xx (copre THINK)
- `_escalate_anthropic()` (rescue): delay hardcoded `[1.5,3.0]` → backoff certificato

### glm puro — streaming + hardening limiter

- Streaming passthrough diretto (fix 2026-07-21): `forward_glm(..., passthrough=True)` relayato senza bufferizzazione
- Hardening rate limiter: `limiter.consume()` protetto con try/except, nessun 500 se Redis down
- Clamp `max_tokens` a 32768: z.ai rifiuta >32768 con 400 `[1210]`

### Regola wiki-ops per-modalità

- Esecutore per wiki = catena modalità attiva:
  - `anthropic` → Haiku
  - `minimax` → m3-wiki
  - `glm` → tier GLM
  - `mix-am/mix-gm` → m3-wiki
  - `mix-ag` → tier GLM

### Audit 6 modalità (3 pure OK, miste parziali con fix)

| Modalità | Status | Note |
|----------|--------|------|
| `anthropic` | ✅ | retry 429/5xx esteso |
| `minimax` | ✅ | passthrough streaming |
| `glm` | ✅ | clamp max_tokens, limiter hardening |
| `mix-am` | ✅ | retry Anthropic, BYPASS-THINK rimosso |
| `mix-ag` | ✅ | retry Anthropic, streaming passthrough |
| `mix-gm` | ✅ | fix prefissi warning, streaming passthrough |

### Leak WebSearch/WebFetch non brandizzati

- `_ANTHROPIC_CLIENT_TOOL_NAMES = {"websearch", "webfetch", ...}` in `tool_isolation.py`
- Fix: search in tutto il message per trovare tool name anywhere nel block

### SSE miste + fix mix-gm prefissi warning

- Prefissi `[VERIFY-WARNING]`/`[HHEM-WARNING]` rompevano lo stream JSON
- Fix: header dedicato `x-router-warn` + estrazione testo SSE via `_extract_text_from_sse()`

### executor mix cieco a system/piano/immagini (2 bugfix)

1. **system blindness**: `remap_body_for_minimax` non convertiva campo `system` top-level
   - Fix: `_inject_system_as_message()` nel body ACT
2. **immagini blindness**: `_strip_images_from_messages` rimuoveva silenziosamente
   - Fix: marker testuale esplicito `[IMMAGINE ALLEGATA DALL'UTENTE]`

### resize immagini clipboard rimosso

- `_shrink_images_in_messages()` rimpiccioliva a 1024px/70% PRIMA del THINK
- Fix: rimossa chiamata da `_build_think_body`, `_shrink_and_retry_minimax`, `_try_shrink_body_haiku`
- Immagini inviate originali al THINK (Anthropic Haiku legge tutto)

---

## 🔑 Decisioni chiave

| # | Decisione | Rationale |
|---|-----------|-----------|
| 1 | **BYPASS-THINK rimosso** | Ogni richiesta mix-am attraversa THINK→ACT→VERIFY. L'orchestratore Anthropic legge tutto (immagini, audio, video, messaggi) prima di delegare. |
| 2 | **Immagini non ridimensionate** | Inviate originali al THINK invece di resize 1024px/70%. Anthropic Haiku deve vedere le immagini in alta risoluzione. |
| 3 | **Regola wiki-ops per-modalità** | L'esecutore wiki è SEMPRE un modello della catena della modalità router attiva — mai un provider terzo. |
| 4 | **Retry Anthropic centralizzato** | Helper `anthropic_call_with_retry()` in `pipeline_common.py` — no duplicazione, certificato SDK. |

---

## ❌ Failed approaches

| Approccio | Problema | Soluzione |
|-----------|----------|-----------|
| mix-gm body JSON corrotto | Prefissi `[VERIFY-WARNING]`/`[HHEM-WARNING]` rompevano lo stream | Header dedicato `x-router-warn` + estrazione testo SSE |
| executor cieco a system | `remap_body_for_minimax` non convertiva campo `system` top-level | `_inject_system_as_message()` nel body ACT |
| executor cieco a immagini | `_strip_images_from_messages` rimuoveva silenziosamente | Marker testuale esplicito `[IMMAGINE ALLEGATA DALL'UTENTE]` |

---

## 📋 TODO attivi

1. **Osservare** — testare il flusso completo con immagini reali (clipboard paste)
2. **Osservare** — verificare che Claude Code non usi più `mcp__MiniMax__understand_image` in modalità anthropic/mix-am

---

## 🔗 Commit log

```
8d3ed77 fix(mix-am): rimuovi resize immagini clipboard — invia originali al THINK
4eeedd9 fix(mix-am): rimossi BYPASS-THINK — ogni richiesta attraversa THINK→ACT→VERIFY
ced4082 fix(mix): rimossi fast-path che bypassavano l'orchestratore Anthropic
5c94a38 fix(mix-am): THINK e VERIFY usano il modello utente selezionato
074eedc fix(glm): clamp max_tokens a 32768 — z.ai rifiuta >32768 con 400 [1210]
```

---

## 📁 File modificati

- `src/pipeline_common.py` — helper retry centralizzato
- `src/ai-router-proxy.py` — retry Anthropic path puro
- `src/pipeline_anthropic.py` — retry THINK/VERIFY, rimozione resize immagini
- `src/pipeline_glm.py` — streaming passthrough, clamp max_tokens
- `src/tool_isolation.py` — brandizzazione WebSearch/WebFetch
- `src/pipeline_glm.py` (mix-gm) — fix prefissi warning
- `src/pipeline_common.py` (executor) — marker immagini, system injection

---

*Generato: 2026-07-22 18:21 — `/wiki all`*
