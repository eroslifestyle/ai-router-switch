# CP_20260718_1515

## Goal
Sessione mista: audit proxy statico + AQ-REF1 completato + rename 3 modalità mixed + fix bug critico dispatch.

## Progress/TODO Board
- [x] AQ-REF1 — StreamingRelay come classe (e8fc50c)
- [x] Rename: mixed→mix-am, anthropic-glm→mix-ag, glm-minimax→mix-gm (18a2644)
- [x] Elimina 2 funzioni orfane (_anthropic_glm_only_chain + _glm_minimax_only_chain)
- [x] Fix MixGM: fallback Anthropic rescue (Haiku→Sonnet→Opus) invece di 502
- [x] **BUG CRITICO FIX**: mode=="mix-am" mancante nel fast-path — il blocco NEW_PIPELINE attivava per TUTTI i modi
- [x] Fix card.py GUI — aggiornati 4 ID ai nuovi nomi (6ce0f9b)
- [x] Router restart pulito (PID 1844602)
- [x] GUI restart (PID 32387)
- [x] Audit statico proxy: resource leak (0), orphan (2 già rimossi), except bare (87 OK), dispatch coverage (6/6 modi)
- [ ] AQ-REF2 — FailTracker centralizzato (SPEC pronta in fail-tracker-SPEC.md)
- [ ] AQ-REF3 — BasePipeline + PipelineConfig
- [ ] AQ-REF4 — providers/base.py
- [ ] AQ-REF5 — pipelines/primitives.py
- [ ] AQ-REF6 — TokenRateLimiter unificato
- [ ] AQ-REF7 — ContextManager
- [ ] AQ-TEST — Test pipeline

## Failed Approaches
- Workflow swarm audit: 5/5 agent falliti per StructuredOutput (nessun JSON valido restituito) — audit statico fatto manualmente

## Do NOT
- NON riavviare router senza kill -9 del processo + restart nohup
- NON usare `cat << EOF >> file` per append vault log (race condition MCP)
- NON eliminare `src/ai-router-proxy.py` dal repo (symlink vs copia viva)
- NON committare secrets o .env
- Router vivo: `/home/mrxxx/.claude/scripts/ai-router-proxy.py` (NON sincronizzato automaticamente col repo)

## Key Decisions
- 3 modi mixed rinominati: mix-am (AM orch/AM esegue) / mix-ag (AM orch/GLM esegue) / mix-gm (GLM orch/MM esegue)
- MixGM fallback: da 502 → _anthropic_rescue (Haiku→Sonnet→Opus) per recovery completo
- Fallimento audit swarm: workaround audit manuale grep/Read mirato più efficace su codice mono-file

## Current State
- Router live: PID 1844602 su 127.0.0.1:8787, mode mix-gm (chat ha MixAM)
- GUI live: PID 32387
- HEAD: 6ce0f9b su main, pushato
- Proxy src/ sincronizzato: `/home/mrxxx/.claude/scripts/ai-router-proxy.py` = `/mnt/backup/.../src/ai-router-proxy.py`

## File Changes (sessione)
| File | Action |
|------|--------|
| `streaming_relay.py` | Creato (322 LOC, AQ-REF1) |
| `streaming-relay-SPEC.md` | Creato |
| `ai-router-proxy.py` (src + live) | Modificato (rename modi + bug fix + elimina orfani) |
| `mode-rename-SPEC.md` | Creato |
| `router-mode/card.py` | Modificato (fix ID pulsanti) |

## Code Context
```python
# VALID_MODES dopo rename
VALID_MODES = ("anthropic", "minimax", "mix-am", "mix-ag", "mix-gm", "glm")

# BUG FIX — riga ~3927: mode check ESPLICITO (PRIMA mancante)
if mode == "mix-am" and NEW_PIPELINE and is_messages and not anthropic_leads:
    # fast-path per mix-am

# _anthropic_rescue — nuovo wrapper fallback Anthropic completo
async def _anthropic_rescue(request, orig, session, chat_fp, relay):
    return await _mixed_haiku_rescue(request, orig, session, chat_fp, relay)

# Routing 6 modi in handle()
3525: if mode == "glm":        → _handle_glm_mode()
3536: if mode == "mix-ag":     → _anthropic_glm_think_act_verify()
3542: if mode == "mix-gm":     → _glm_minimax_think_act_verify()
3734: if mode == "anthropic":  → forward_anthropic()
3813: if mode == "minimax":    → forward_minimax()
3927: if mode == "mix-am" ...: → NEW_PIPELINE / _pipeline_think_act()
```

## Resume Instructions
Aprire nuova sessione su `/mnt/backup/Dropbox/1 Programmazione/Progetti/ai-router-switch/`. Prossimo passo: AQ-REF2 — FailTracker centralizzato. SPEC in `fail-tracker-SPEC.md`. Lo stato fail tracking attuale è `_mixed_fails` / `_mixed_fail_ts` / `_mixed_cooldown_until` con lock `_counter_lock` (r 617-685 del proxy). L'obiettivo è estrarre in `fail_tracker.py`.

## Warnings
- Proxy vivo (`~/.claude/scripts/`) NON è git-tracked automaticamente: dopo ogni modifica significativa va copiato in `src/` e committato
- Il refactor StreamingRelay ha cambiato la struttura di handle() (273 righe rimosse, sostituite con istanza + bound method) — il proxy live ha la modifica ma il router non è stato restartato da quando lo streaming_relay.py è stato creato
