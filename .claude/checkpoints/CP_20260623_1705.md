# CP_20260623_1705 — ai-router-switch audit completo + fix port comments

## Goal

Audit completo dello stack AI Router Switch (3 servizi + 4 fasi + 5 porte) e fix dei gap #1 e #3
(allineamento porta `:8787` nei commenti di `systemd/ai-router.service` e `scripts/headroom-minimax.sh`).

## Progress / TODO Board

### ✅ Done (verificato)
- Struttura progetto esplorata: `src/ai-router-proxy.py` (677 righe), `scripts/`, `systemd/`, `docs/PIANO.md`
- 3 servizi systemd verificati attivi (`:8787` ai-router, `:8790` headroom#2, `:8791` headroom#1) — `active (running)`, enabled, `Linger=yes`
- Cron watchdog verificato: `* * * * * ai-stack-guard.sh` + `@reboot`, ha recuperato headroom#2 in passato
- 5 porte live testate (8787 dinamica + 8771/8772/8773/8774 forced-mode) — instradamento corretto per ciascuna
- Fase 1 (porte deterministe) verificata — `:8771`→anthropic, `:8772`→minimax, `:8773`→mixed, `:8774`→interactive
- Fase 2 (per-chat fingerprint) verificata end-to-end con test realistico (vedi Failed Approaches per il falso allarme)
- Fase 3 (comandi in-chat) verificata: `!router {anthropic|minimax|mixed|interactive|status|reset|help}` + frasi naturali italiane
- Fase 4 (circuit breaker) verificata per code-review: `BREAKER_FAIL_MAX=3`, `BREAKER_COOLDOWN=120s`, `FALLBACK_STATUSES={429,500,502,503,504,529}`
- Modalità `interactive` verificata: header `x-ai-verified: opus|unavailable` presente, T2 classifier funziona
- Gap #1 fixato: `systemd/ai-router.service` Description aggiornata a "porta 8787"
- Gap #3 fixato: `scripts/headroom-minimax.sh` commento aggiornato a `:8787`
- Commit `fd33664` pushato su `main` (2 file, +2/-2 righe)
- `/wiki all` step 1+3 eseguiti in modalità delegata (haiku): 2 pagine vault create, codemap rigenerato, commit vault serializzato OK

### ⬜ To-Do (prossima sessione)
- [ ] Valutare symlink `src/ai-router-proxy.py` → `~/.claude/scripts/ai-router-proxy.py` per evitare drift (copia separata attuale)
- [ ] Indagare perché watchdog ogni tanto cade su systemd per headroom#2 (cade su nohup, storico recente: 2026-06-23T16:53)
- [ ] (opzionale) Gap #2 — aggiornare `scripts/ai-mode` commento da "porta 8789" a "porta 8787" — non fixato per scelta utente

## Failed Approaches

### ❌ Test marca-chat con system prompt diverso tra marca e payload
Sintomo: marca `!router anthropic` salvata su fp `9eb7d19e4a79` (system vuoto), poi payload con `system="Sei un eco..."` → fp calcolata `6caaade84f34` → marca non si applica → instradamento a `minimax` (default).
Root cause: la `conversation_fingerprint()` include `system + first_user`. Cambiando il system, la fp cambia. **NON è un bug**: è by-design, coerente con l'uso reale di Claude Code dove il primo messaggio utente è stabile in tutta la chat.
**Lezione**: per testare la Fase 2, mantenere IDENTICI `system` e `first_user` tra marca e payload di test.

### ❌ Test marca-chat con `ts: 1.0` nel JSON scritto a mano
Sintomo: ho scritto direttamente in `~/.claude/ai-router-chats.json` l'entry `{fp: {mode, ts: 1.0}}` → al primo `_load_chats()` il TTL 7 giorni ha cancellato l'entry (`1.0 < now - 7*86400`).
**Lezione**: per testare, usare il flusso normale (comando `!router`) che scrive `ts = time.time()`.

## Do NOT

- **NON** killare / disabilitare i 3 servizi protetti senza ripristino immediato (regola globale `AI-ROUTER-POLICY.md`).
- **NON** modificare la porta reale `:8787` nei file di deploy (`/home/mrxxx/.claude/scripts/ai-router-proxy.py` è la copia attiva, NON `src/`).
- **NON** iniettare `x-api-key` Anthropic nel router — il router passa trasparentemente gli header del client (policy "router non inietta segreti"). Se il client non ha una chiave valida Anthropic, la modalità `interactive` (T2) restituirà `x-ai-verified: unavailable` (atteso).
- **NON** provare a testare la Fase 4 (circuit breaker) killando headroom#1 in produzione — logica verificata per code-review, test runtime rimandato a manutenzione.
- **NON** dimenticare la cache `_chat_cache` (5s) in `_load_chats()` — modifica esterna al file `ai-router-chats.json` non si vede per 5s.

## Key Decisions

- **Drift `src/` vs `~/.claude/scripts/`**: attualmente sono due copie separate. La copia deployata (`/home/mrxxx/.claude/scripts/ai-router-proxy.py`) NON è un symlink. Fix non urgente ma rischioso per il futuro.
- **Audit 2026-06-23**: tutte e 4 le fasi sono operative. Nessun bug critico. Solo gap cosmetici.
- **In modalità `interactive` la verifica Opus dipende dall'auth del client**: by-design (router trasparente). Workaround per utente: assicurarsi che Claude Code punti a `:8787` con `x-api-key` Anthropic valido nel proprio settings.json (opzionale, dipende dal workflow).

## Current State

- Repo: `main` @ `fd33664` (clean working tree)
- Stack runtime: tutti e 3 i servizi attivi, modalità corrente = `minimax`
- Vault: 2 nuove pagine + 1 append a `log.md` + codemap rigenerato
- Nessuna operazione pendente

## File Changes

```
systemd/ai-router.service   | 2 +-
scripts/headroom-minimax.sh | 2 +-
.gitignore                  | +2 (graphify-out escluso)  [da hook esterno]
2 files changed, 2 insertions(+), 2 deletions(-)  (più ignore)
```

## Code Context

### Porte (da `src/ai-router-proxy.py`)
```python
LISTEN_PORT = int(os.environ.get("AIROUTER_PORT", "8787"))  # porta dinamica
PORT_MODE = {
    8771: "anthropic",
    8772: "minimax",
    8773: "mixed",
    8774: "interactive",
}
LISTEN_PORTS = [int(os.environ.get("AIROUTER_PORT", "8787"))] + list(PORT_MODE.keys())
```

### Circuit breaker (Fase 4)
```python
BREAKER_FAIL_MAX = 3
BREAKER_COOLDOWN = 120  # secondi
FALLBACK_STATUSES = {429, 500, 502, 503, 504, 529}
_breaker = {"anthropic": {"fails": 0, "open_until": 0},
            "minimax": {"fails": 0, "open_until": 0}}
```

### Per-chat fingerprint (Fase 2)
```python
def conversation_fingerprint(data: dict) -> str:
    system = data.get("system", "")
    if isinstance(system, list):
        system = " ".join(b.get("text", "") for b in system if isinstance(b, dict))
    first_user = ""
    for m in data.get("messages", []):
        if m.get("role") == "user":
            c = m.get("content", "")
            first_user = c if isinstance(c, str) else " ".join(b.get("text", "") for b in c if isinstance(b, dict))
            break
    return hashlib.sha256((str(system) + "||" + str(first_user)).encode()).hexdigest()[:12]
```

### Comandi in-chat (Fase 3) — solo porta dinamica `:8787`
```python
if forced is None and request.path.endswith("/v1/messages"):
    # parse_router_command(_last) — esplicito (!router X) o naturale (verbo italiano)
    # marca-chat salvata in CHAT_STORE (TTL 7 giorni)
```

## Setup

- Stack già attivo (systemd + cron + hook). Nessuna installazione richiesta per riprendere.
- Modalità dinamica corrente = `minimax` (cambia con `ai-mode {anthropic|minimax|mixed|interactive}`).
- API keys: Anthropic via `x-api-key` del client, MiniMax letto runtime da `~/.claude/secrets/secrets.sh` (60s cache).

## Resume Instructions

1. Leggi questo checkpoint + la pagina vault `progetti/ai-router-switch/sessioni/2026-06-23 - audit completo e fix port comments.md`
2. Controlla `git status` + `git log --oneline -5` sul repo
3. Lo stack è già attivo: testa con `ai-mode status` e curl su `:8787/__router_health`
4. I 3 TODO principali sono in ordine di priorità decrescente (symlink drift → watchdog headroom#2 → gap #2 script ai-mode)

## Warnings

- ⚠️ Context di questa sessione era a 154K quando `/wiki all` è partito → modalità delegata (lever B) usata per step 1+3.
- ⚠️ Il file `.gitignore` è stato aggiornato da hook esterno per ignorare `graphify-out/` (corretto: artefatto AST locale).
- ⚠️ `/home/mrxxx/.claude/scripts/ai-router-proxy.py` (deploy) ≠ `src/ai-router-proxy.py` (repo): copia separata. Modifiche a uno non si propagano all'altro.
