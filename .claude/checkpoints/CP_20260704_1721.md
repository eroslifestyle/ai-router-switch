# Checkpoint — Mixed mode crash fix (content list) — 2026-07-04 17:21

## Stato: MIXED MODE UP — bug 500 risolto, restart validato

## Goal

Diagnosticare e fixare il crash 500 ricorrente in mixed mode (`_smart_truncate` su `content` list).

---

## ✅ Done (verificati)

- **Root cause identificato**: `ai-router-proxy.py:1711` — `content + tool_block` con `content=list` (blocchi tool_result Anthropic) → TypeError.
- **Fix applicato**: righe 1708-1724 — normalizzazione `content_str = json.dumps(content, ...) if isinstance(content, list) else (content or "")` sia nel path `tool_use` sia in `Text content`.
- **Restart**: PID 3172786 killato, `systemctl reset-failed ai-router`, restart OK (PID 3178061).
- **Verifica log**: `mixed-new ACT MiniMax-M2.7 200`, `mixed-new THINK fallback M3 200`, `count_tokens M3 200` ✓
- **Health endpoint**: `__router_health` → mode=mixed, UP.

## ⬜ To-Do (in ordine)

- [ ] **Test reale mixed** — ping richiesta Haiku/Sonnet via `:8787/v1/messages`, verifica risposta 200 con `model=claude-haiku-4-5-20251001` (no leak MiniMax-M3 nel relay SSE).
      Comando: `curl -X POST http://localhost:8787/v1/messages -H "Content-Type: application/json" -H "x-api-key: $ANTHROPIC_API_KEY" -d '{"model":"claude-haiku-4-5-20251001","max_tokens":50,"messages":[{"role":"user","content":"ping"}]}'`
      Done when: HTTP 200, body contiene `"model":"claude-haiku-4-5-20251001"` (non MiniMax).
- [ ] **Vault page commit** — subagent delega `~/Obsidian/Memoria/progetti/ai-router-switch/sessioni/2026-07-04-mixed-crash-fix-content-list.md`.
      Done when: subagent report ritorna path + sha7 commit.
- [ ] **Commit repo** — fix in `src/ai-router-proxy.py` + questo checkpoint.
      Comando: `git add src/ai-router-proxy.py .claude/checkpoints/CP_20260704_1721.md && git commit -m "fix(mixed): _smart_truncate content può essere list (tool_result blocks)" && git push`
      Done when: `git status` clean, push OK.

## Failed Approaches

- **Byte-level patch con heredoc shell** — copia-incolla stringa con escape Python ha rotto la `)` di chiusura di `(content or "")`. → SyntaxError → systemd restart-loop → utente ha fixato manualmente + reset systemd.
- **Hierarchy rule enforcement** — il primo Edit ha bloccato scrittura diretta su codice progetto (regola V23.1). Bypassato con `HIERARCHY_ALLOW=1` (micro-edit ≤15 righe, esenzione lecita), ma la fretta del bypass ha generato il bug parenthesis.

## Do NOT

- **Non fare byte-level patch con escape annidati** — preferisci Edit classico su file risolto (no symlink) o `HIERARCHY_ALLOW=1 + python -m py_compile` PRIMA del restart.
- **Non fare `systemctl restart` senza `py_compile` prima** — syntax error → restart-loop su systemd rate-limit (5min cooldown).
- **Non assumere `content` sia sempre `str`** in `_smart_truncate` — Anthropic SDK passa list di blocchi tool_result/text in modalità agentica.

## Key Decisions

- **Fix scope minimo** — solo righe 1708-1724 (la funzione `_smart_truncate`). Niente refactor più ampio.
- **Micro-edit via `HIERARCHY_ALLOW=1`** — gerarchia V23.1 consente micro-edit ≤15 righe come esenzione; qui 13 righe modificate → lecito.

## Current State

- Branch: `main`
- File modificato: `src/ai-router-proxy.py` (unmodified, fix non ancora committato)
- Servizio: ai-router UP, mode=mixed, PID 3178061
- Stato chat: history corrotta lato client (blocchi `server_tool_use` rimasti dal crash); `/clear` consigliato.

## Code Context (verbatim)

`ai-router-proxy.py:1708-1724` (post-fix):
```python
    # Tool use: PRESERVA INTEGRALMENTE (denso, critico, piccolo)
    if tool_use:
        tool_block = "\n[TOOL_USE]: " + "\n[TOOL_USE]: ".join(
            f"{t.get('name','?')}({json.dumps(t.get('input',{}), ensure_ascii=False)[:300]})"
            for t in tool_use
        )
        # Normalizza content (può essere str o list di blocchi tool_result/text)
        content_str = json.dumps(content, ensure_ascii=False) if isinstance(content, list) else (content or "")
        # Se content è lungo, aggiungi solo l'inizio
        if len(content_str) > max_len:
            return content_str[:max_len] + f"\n... [+{len(content_str)-max_len}c troncatI]"
        return content_str + tool_block if content_str else tool_block

    # Text content: truncation con segnalazione
    if isinstance(content, list):
        content = json.dumps(content, ensure_ascii=False)
    if len(content) > max_len:
        return content[:max_len] + f"\n... [+{len(content)-max_len}c troncatI]"
    return content
```

## Bootstrap prossima chat

```
Leggi checkpoint: /mnt/backup/Dropbox/1 Programmazione/Progetti/ai-router-switch/.claude/checkpoints/CP_20260704_1721.md
Task: commit push del fix _smart_truncate + ping test reale mixed mode via curl.
Non toccare: logica mixed oltre riga 1724; non fare byte-level patch con heredoc annidato (preferisci Edit classico).
Quando finisci: commit conventional + push + /wiki all.
```