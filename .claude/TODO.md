# ai-router-switch — TODO unico

**Aggiornato 2026-08-04 alle 04:35; HEAD di riferimento 10ac133; stato 10 voci aperte, di cui 3 di sola osservazione e 2 opzionali, e 22 chiuse; suite 257 passed dalla root; ai-router e ai-router-self-healing attivi; timer ai-router-self-fix con prima corsa alle 09:31; otto porte a 200 (8787 più 8771-8773 e 8775-8778); modalità globale mix-am.**

il 2026-08-04 le sette sessioni del 3 e 4 agosto sono state fuse nel checkpoint unico `.claude/checkpoints/CP_20260804_0435.md`, che è il punto di ripresa; la narrazione del periodo 27/07-03/08 resta in `.claude/WIKI.md`; questo file resta la sola lista operativa. Il dettaglio verboso delle voci chiuse prima di oggi resta recuperabile con: `git show 10ac133:.claude/TODO.md`

## Aperti

Prima voce di tutte, non una voce di lavoro ma un'osservazione datata: alle 09:31 del 2026-08-04 gira per la prima volta il self-fixer L2 (timer ogni 6 ore). Esito atteso in `~/.claude/logs/self-fix.log` e stato del ticket in `~/.claude/self-fix/`; il primo candidato è il ticket `minimax_context_exceed` con 142 occorrenze; esiti possibili `merged`, `failed`, `reverted`, `skip`.

- [ ] **Passthrough dei blocchi `tool_reference` (tool deferral di Claude Code). Emersa il 2026-08-04.** Il repo non ha una sola occorrenza di `tool_reference`: finché è così, `ENABLE_TOOL_SEARCH` deve restare disattivata (commentata in `~/.bashrc` e `~/.config/environment.d/anthropic.conf`), altrimenti i tool deferiti — Read, Bash, Edit, Write, Agent — diventano irrecuperabili dietro il router. Rilevatore già in produzione: `tools_builtin` nel sidecar, popolato sotto `BUILTIN_ANOMALY_THRESHOLD = 20`.

- [ ] **(opzionale) Fascia peak GLM 14–18 Asia/Shanghai dal vivo.** La correttezza è già coperta in modo deterministico da `sviluppo/tests/test_peak_scheduler.py`: quattro bordi con ora iniettata, off-by-one catturato dalla prova del nove. Zero righe `peak-cap` nei log perché le modalità GLM sono poco usate.
  **Expected outcome:** nessuna garanzia aggiuntiva sulla correttezza; solo conferma su traffico reale.

- [ ] **Triage dei finding codeburn — scelto `mcp-low-coverage`, ma si misura prima di disattivare.**
  **Contesto.** Audit codeburn del 2026-07-27, sola lettura, 30 giorni: health **F 20/100**, 855 sessioni, risparmio potenziale dichiarato 41,3% pari a 1,98 miliardi di token. I tre finding principali: `low-worth-sessions` 923M token, `context-heavy-sessions` 674M, `mcp-low-coverage` 315M.
  **Scelta.** Il primo finding su cui intervenire è `mcp-low-coverage`, nonostante gli altri due abbiano numeri assoluti più alti.
  **Perché questo per primo.** È l'unico dei tre con una leva di configurazione deterministica e reversibile: un server MCP è caricato oppure no, e i suoi schemi pesano su ogni richiesta. Gli altri due non lo sono. `context-heavy-sessions` (674M) è già stato attaccato dalla campagna context rate del 27–28 luglio (F1, F8–F11) e dal gate di contesto: il margine residuo è più piccolo di quanto il numero suggerisca. `low-worth-sessions` (923M) è il più grande ma non ha una leva tecnica singola: richiede un giudizio su cosa sia una sessione a basso valore, quindi un cambio di abitudini, non una modifica.
  **Fatti verificati oggi (2026-07-28).** Tre, e due cambiano il quadro. Primo: **`debinex` non esiste più nella configurazione** — zero occorrenze in `~/.claude.json`. Il finding «debinex 0 tool su 76, in 81 sessioni» è quindi obsoleto e non va più contato. Secondo: i connettori restano attivi **a livello di account, non di progetto**. Il campo `claudeAiMcpEverConnected` elenca Gmail, Google Drive, Google Calendar, Canva e Claude Code Remote; i server MCP locali in `~/.claude.json` sono due, `MiniMax` e `zai`; in `settings.json` risultano disabilitati `mcp-video` e `nuclear-arsenal`. I connettori Google vengono quindi caricati anche in sessioni che non c'entrano nulla, come quelle su questo proxy. Terzo: **il peso del blocco `tools` non è oggi misurato da nessuna parte.** I campi del sidecar `router-usage.jsonl` sono `ts, chat, orig, final, mode, client, status, input_tokens, output_tokens, cache_read, cache_creation`; [`src/router_debug.py:156`](../src/router_debug.py#L156) registra soltanto `has_tools`, un booleano.
  **Perché misurare prima di agire.** I 315M sono una stima di codeburn su euristiche proprie, **mai verificata sul nostro traffico**. Questo progetto ha già pagato due volte il prezzo di agire su numeri non verificati: la baseline delle 5.215 richieste si è rivelata un artefatto di misura, e i 502 del passthrough sono rimasti invisibili per giorni perché non finivano nel sidecar. Il router è nella posizione ideale per misurare davvero: vede il blocco `tools` di ogni richiesta, e lo ispeziona già in `src/tool_isolation.py` e `src/minimax_body.py`. Distribuzione attuale degli input su 117.805 richieste con `input_tokens` valorizzato: mediana 27.221 token, con un picco di 15.137 richieste nella fascia fra 14.000 e 16.000, che è il costo fisso tipico di una sessione.
  **Strumentazione: FATTA** (commit `066179a`). `collect_tools_stats()` in [`src/router_utils.py`](../src/router_utils.py), agganciata al logging centrale `streaming_relay.py:363` — quel file sta nella **ROOT** del repo, non in `src/`. Dietro `AIROUTER_TOOLS_TELEMETRY`, **default spento**: a flag spento la funzione esce prima di qualsiasi parsing e l'entry del sidecar resta identica, così i consumatori esistenti non vedono campi nuovi. 8 test nuovi, suite 120 → **128 passed**.
  **Misura eseguita (2026-07-28).** Attivata sul traffico reale con un drop-in systemd. Su una richiesta tipica di Claude Code: **64 tool per 142.056 byte**, pari a circa **56.822 token** stimati con lo stimatore del progetto ([`src/token_counter.py`](../src/token_counter.py), 2,5 byte/token sui modelli Anthropic nuovi — **non** la stima `char/4`, che era la causa del bug F9). Di questi, 40 tool sono MCP per 70.944 byte. Ripartizione per server: `claude_ai_Gmail` 32.379 byte ≈ 12.951 token (22,8% del blocco); `claude_ai_Google_Calendar` 26.378 byte ≈ 10.551 token (18,6%); `claude_ai_Google_Drive` 10.795 byte ≈ 4.318 token (7,6%); `zai` 1.351 byte ≈ 540 token (1,0%). I tool nativi non MCP sono 71.112 byte, il 50,1%. I tre connettori Google insieme valgono 69.552 byte, cioè **≈ 27.820 token, il 49,0%** del blocco `tools`.
  **Il dato che ridimensiona il finding.** Quei token sono **quasi sempre coperti dal prompt caching**. Su 9 richieste osservate, 7 hanno `input_tokens` pari a 2 e `cache_read` fra 116.037 e 244.049; solo 2 pagano il prompt pieno, e sono i cache miss. Il costo per richiesta è quindi di ≈ 27.820 token pieni **sul cache miss**, ma di ≈ 2.782 token equivalenti **sul cache hit**, dove `cache_read` costa un decimo dell'input.
  **Decisione che ne consegue.** La stima esterna di 315M token va considerata un **limite superiore teorico, non un risparmio disponibile**: presuppone che ogni richiesta paghi i tool a prezzo pieno, mentre il caching li copre quasi sempre. Resta reale il costo **non fatturabile**: i 27.820 token occupano spazio nella finestra di contesto di ogni richiesta, cache o no. Su un modello da 200.000 token come Haiku sono il **13,9% della finestra** speso in tool che l'audit indica come non usati (Gmail 1 tool su 16 in 702 sessioni, Calendar 0 su 11).
  **Tentativo di disattivazione via `disabledMcpServers`: NON FUNZIONA sui connettori claude.ai. Smentito dalla misura il 2026-07-29.** Il 29/07 alle 09:26 `~/.claude/settings.json` è stato modificato aggiungendo `disabledMcpServers: ["claude.ai Gmail", "claude.ai Google Calendar", "claude.ai Google Drive"]`, con i nomi esatti mostrati da `claude mcp list` (chiave diversa da `disabledMcpjsonServers`, che vale solo per i server da `.mcp.json`). La chiave è ancora lì, verificata rileggendo il file.
  **Misura che la smentisce.** Sidecar `router-usage.jsonl`, entry con `tools_mcp_servers` successive alle 09:27: **132 su 132 contengono ancora tutti e tre i connettori** (`zai` in 130). Ultima entry, `2026-07-29 17:47:01`: 58 tool, 137.243 byte, `{"claude_ai_Gmail": 32379, "claude_ai_Google_Calendar": 21571, "claude_ai_Google_Drive": 10795, "zai": 1351}`. Il calo di `tools_bytes` da 142.056 a 137.243 non è la rimozione dei connettori: sono −4.813 byte, quasi tutti dovuti a Calendar che è passato da 26.378 a 21.571 (schema del connettore cambiato). I 69.552 byte attesi non se ne sono andati.
  **Ipotesi «config non riletta» esclusa.** Il processo di questa sessione (PID 2304879, `claude` dell'estensione VSCode) è partito alle **17:41:47**, otto ore dopo l'mtime di `settings.json` (09:26:30). Ha letto la chiave e ha caricato i connettori lo stesso. Riscontro indipendente: i tool `mcp__claude_ai_Gmail__*`, `mcp__claude_ai_Google_Calendar__*` e `mcp__claude_ai_Google_Drive__*` sono presenti nel contesto della sessione avviata dopo la modifica.
  **Conclusione.** La prova statica dal bundle (`Nw(nome)` che ritornerebbe `{type:"disabled", tools:[]}` prima della discriminazione di tipo) è **contraddetta dal comportamento reale**: quel percorso non copre i connettori `claudeai-proxy`, oppure i loro tool entrano nel contesto per un'altra strada. **Ripiego certo, da fare a mano nel browser:** disconnettere i tre connettori da `https://claude.ai/customize/connectors`. La chiave in `settings.json` può restare (innocua) o essere rimossa; non serve a niente per questi server.
  **Cosa NON prova la disattivazione** (già verificato, non ripetere): `claude mcp remove` risponde «No MCP server named», conosce solo i server locali; `claude mcp logout` rimanda al browser perché riguarda le credenziali, non il caricamento; `claude mcp list` li mostra `✔ Connected` anche da disabilitati, perché fa un health check della configurazione; `claude -p` headless non carica MCP a prescindere (controprova con lista vuota: risultato identico).
  **Telemetria.** Resta accesa per raccogliere un campione più ampio — oggi il dato per-server viene da 320 entry ma da poche sessioni. Per spegnerla: cancellare `~/.config/systemd/user/ai-router.service.d/tools-telemetry.conf`, poi `systemctl --user daemon-reload` e riavviare con la procedura completa. Vincolo che resta valido: non eseguire `codeburn --apply` né `codeburn guard install`.

- [ ] **Disconnettere dal browser i tre connettori Google. Emersa il 2026-07-29, riaperta dalla misura.**
  Mettere `claude.ai Gmail`, `claude.ai Google Calendar` e `claude.ai Google Drive` in `disabledMcpServers` dentro `~/.claude/settings.json` non li disattiva: la misura dinamica lo ha smentito, i tool continuano a essere caricati. I tre insieme pesano 69.552 byte, circa 27.820 token, il 49,0% del blocco tools. Il costo in fattura è però quasi tutto assorbito dal prompt caching, quindi l'urgenza è bassa: quello che resta è occupazione di finestra, il 13,9% su un modello da 200.000 token come Haiku. L'unica strada verificata è disconnetterli manualmente da `https://claude.ai/customize/connectors`.
  **Expected outcome:** dopo la disconnessione, `tools_mcp_servers` nel sidecar `router-usage.jsonl` elenca solo `zai`, e `tools_bytes` cala di circa 69.552 byte.

- [ ] **Misurare l'effetto delle tre leve sull'enforcement. Applicate il 2026-08-03, effetto da verificare fra qualche giorno.** Diagnosi: in `mix-am` Anthropic consuma **9,3×** MiniMax sull'input equivalente (299,0M contro 32,1M, cache pesata al 10%) mentre l'output è rovesciato (MiniMax 13,0M contro 7,65M). Il 38,6% delle richieste Anthropic era `coding` (3.018 contro 168) e l'enforcement bloccava il 3% delle scritture (2.202 allowed contro 70 denied). Leve applicate a `~/.claude/hooks/enforce_hierarchy.py`: estensioni web/shell/config in `CODE_EXTENSIONS` (`.html` da solo valeva 657 eventi), soglia micro-edit 15→5 nelle mode deleganti, `.md` come codice solo dove si delega con deny verso `m3-wiki`. Test `~/.claude/scripts/test_enforce_hierarchy_leve.py` 16/16.
  **Expected outcome:** su una nuova finestra di 7 giorni, la quota `task_class=coding` su Anthropic scende sotto il 38,6% e le `denied` in `hierarchy-violations.jsonl` salgono ben sopra le 70. Attenzione: la quota **strutturale** (~226k token di contesto per turno del main contro 61k dell'esecutore) **non** è aggredibile per questa via — se il rapporto non migliora abbastanza, la leva successiva è ridurre i round-trip del main, non aumentare le deleghe.

- [ ] **Taratura dell'heartbeat sul traffico vero.** `CTX_GATE_HEARTBEAT_PCT = 0.30` (`src/ai-router-proxy.py:145`, env `AIROUTER_CTX_HEARTBEAT_PCT`).
  **Prossimo step:** se dopo una settimana il catalogo resta senza entry `ctx_gate`, abbassare la soglia. Costo nullo: il catalogo deduplica per (modalità, azione) e il throttle resta 300 s per chat.

- [ ] **Osservare la salute del prompt caching nei prossimi giorni. Emersa il 2026-08-01.** (di sola osservazione)
  Dopo il fix dei due bug sui `cache_control`, la firma di salute è la riga `cache: OK bp=...` nel log del router con `input=2`. La patologia da riconoscere è `creation=0` con `input` a sei cifre, che significa prompt caching di nuovo morto. Se ricompare, ispezionare prima la coda dei messaggi e poi l'output di `filter_tools_for_backend`: il conteggio `bp=s2/m1/t0` dice subito quale sezione ha perso il breakpoint.
  **Expected outcome:** nessun intervento finché la firma resta sana; se ricompare la patologia, riaprire con il log a supporto.

- [ ] **Tenere monitorato il limite latente `sock_read=120` sugli stream. Emersa il 2026-08-01.** (di sola osservazione)
  Le richieste non-streaming sono passate a 600 secondi con il fix `7c84535`, mentre lo streaming eredita ancora i 120 secondi della sessione condivisa, dove quel valore protegge davvero dagli stall. Le 9 occorrenze storiche in modalità anthropic sono tutte `SSE=False` e precedenti al fix.
  **Expected outcome:** nessun intervento finché non compare un caso con `SSE=True` posteriore al fix.

- [ ] **(opzionale) Alzare `TRUNCATE_MAX_LEN` per usare più del 43% della finestra.** Emersa il 2026-08-02. Dopo la conversione del budget da token a caratteri, un body da 1135KB consegna all'esecutore 86.944 token su un `safe_limit` di 196.608, cioè il **43%**. Il resto è vincolato da `TRUNCATE_MAX_LEN = 1800` (`src/trim_smart.py`), il tetto per singolo messaggio dentro il riassunto — che è anche l'unità con cui `_smart_sample_middle` stima il costo quando applica il budget.
  **Perché non è stato fatto ora:** alzarlo cambia la qualità del riassunto (messaggi più lunghi ma meno numerosi, oppure riassunti oltre il budget) in modi che non sono stati misurati. Non è un difetto aperto: è un margine documentato.
  **Prossimo step:** misurare la qualità del riassunto a `TRUNCATE_MAX_LEN` 1800 / 3000 / 5000 su una conversazione reale lunga, verificando che (a) il budget resti rispettato e monotono — proprietà da cui dipende la convergenza dei loop di shrink nei tre percorsi — e (b) l'informazione critica di inizio conversazione sopravviva. **Expected outcome:** una soglia scelta con il dato a supporto, oppure la conferma motivata di 1800.

- [ ] **Guardia tool stranieri: da rilevazione a filtro.** La guardia response-side (`a85e505`) oggi rileva e non filtra, per scelta: riscrivere uno stream SSE romperebbe la numerazione dei `content_block`.
  **Sblocco:** un caso reale, `grep FOREIGN-TOOLUSE ~/.claude/logs/ai-router.log`. **Expected outcome:** finché il grep è vuoto, nessuna azione — un filtro su un fenomeno mai osservato non sarebbe verificabile.

## Chiusi — indice per fase

- 19 lug — stabilizzazione. Crash-loop UnboundLocalError (`80b6ab5`), root cause dei 404 MiniMax uguale header Host inoltrato (`a5c31af`), isolamento tool centralizzato (`462f181`), retry-storm mix-am con THINK non bloccante (`c3a2ca8`), 3 bug della modalità GLM pura (`6e51322`), sistema di debug centralizzato.
- 21-22 lug — lavori lunghi e modalità miste. TRIM INTERCEPT rimosso (`71497ae`), THINK sintetico su Haiku con VERIFY a campione (`d2bb6aa`), streaming passthrough sulle miste (`cd6b4ef`, `5f6c9f5`), retry 429 certificato (`e6fb4fb`, `e5dc339`, `582eca2`), esecutore non più cieco a system e immagini (`bb84a41`, `447d1e6`), warning spostati nell'header `x-ai-verify` (`3b5a664`), isolamento web search (`a227ea3`).
- 23-25 lug — refactor. Merge totale con WIP worktree scartato come superseded, pipeline morte e codice morto rimossi (`2bd4212`, `0174894`), refactor tunnel con i selettori di modello Anthropic fuori dal router (`99dcc0d`), VERSION_MANIFEST riallineato 7 su 7, `version_drift_check.py` riscritto da stub.
- 26 lug — guardrail e igiene. Guardrail di contesto G1, G2, G3, G5 e G6 (`c58fa1e`, `ada9af7`, `1031dde`, `10de32c`), switch di modalità a voce rimosso, `inverse` e `interactive` eliminati ovunque, 86 override per-chat azzerati, manuali riscritti sui modi reali (`4759195`), id `server_tool_use` sanificati (`a9d039c`), guardia response-side sull'isolamento tool (`a85e505`), `mix-gm` smentito con misura.
- AQ Backlog — chiuso. AQ-RL1, RL2 e FIX1 (`8e40532`), AQ-REF1 (`e8fc50c`), AQ-REF2 (`3a64731`), AQ-REF3, 4 e 5 (`0280326`, poi `src/pipelines/` eliminata dal refactor tunnel), AQ-REF6 (limiter già indipendenti), AQ-REF7 (`70dc5e2`), AQ-TEST (`1e40859`).
- 27-28 lug — context rate e telemetria. G4 chiuso senza azione sul proxy con il marker di compattazione trovato nel binario, 4 test che non venivano mai eseguiti fatti entrare in suite, 502 "Timeout on reading data from socket" sulle non-streaming chiuso con `NON_STREAM_SOCK_READ_SEC` (`7c84535`), Models API scartata a runtime e adottata come controllo offline con una sottostima di 5 volte trovata su `claude-sonnet-4-5`, e la baseline dei "201M token persi" smentita come artefatto di misura. Narrazione in `.claude/WIKI.md`.
- 1-2 ago — stall e system prompt perduto. Stall "Response stalled mid-stream" in mode `anthropic` chiuso: era prompt caching morto, due bug sui `cache_control` (`aeccaaf`, `f158d50`), input non cachato da 226.748 a 2. E in mix-am il system prompt non arrivava MAI all'esecutore MiniMax, più altri dieci difetti su shrink e telemetria (`8cc2922`, `77b311c`, `e757bf3`). Narrazione in `.claude/WIKI.md`.
- 3 ago — merge, qwen e pulizia. Wiki consolidato del periodo (`ca9be80`) e tre leve sull'enforcement dopo la diagnosi del consumo 9,3 volte in mix-am (`c26c624`); settima modalità qwen attiva e verificata in produzione, con `synthetic_response.py` estratto e `test_glm_modes.sh` da 7/5 a 11/0; tre incertezze qwen chiuse a misura (`40708f6`, `130d6ad`, `f8bf84f`) e pulizia del codice morto con 374 righe rimosse (`b9dbe8f`, `a76cd53`, `4dfcae8`, `5ab60b7`).
- 4 ago (notte) — bug hunting e self-healing attivo. `apply_peak_cap` collegata con la mappa inversa (`b1880d5`), famiglia THINK/VERIFY GLM e campionamento AQ-8 rimossi (`095782e`, `9a2243b`), omonime divergenti sul context-check allineate (`91136d2`), tiering per complessita rimosso (`213309e`), `TYPICAL_FILES_BY_MODE` riscritto sulle modalità reali con i test di `router_policy` e `auto_fixer` (`a774a2d`), `classify_outcome` che distingue `thinking-only` da `empty` (`5026027`), watcher del self-healing che non poteva girare (`5bbaa57`), comandi `!router` allineati (`6b76973`) e documentazione portata alle sette modalità (`d7755f6`). I 400 Anthropic residui risultano chiusi dal fix `a9d039c`.
- 4 ago (notte fonda) — self-fixer L2 attivo e quattro bug del router. Il loop si chiude: nessun consumatore di `router-policy.json` esisteva, ora `get_chain` devia l'esecutore degradato (`5545b4a`, `13263c4`); self-fixer L2 con quattro gate, auto-merge e auto-revert, timer 6 ore (`57ae5ef`); parser SSE che ignorava ogni risposta Qwen (`2a9e397`), limiter qwen e strip dei connettori pesanti (`8dd7159`, `f9bb429`), telemetria `tools_builtin` (`a536bab`), README e TODO allineati (`34528fa`, `10ac133`). Causa vera della mancanza dei tool built-in in qwen: `ENABLE_TOOL_SEARCH=auto` nell'ambiente, non il router.

## Note di manutenzione

- Il punto di ripresa è `.claude/checkpoints/CP_20260804_0435.md`, **unico checkpoint rimasto**. I precedenti sono in tre archivi nella stessa cartella: `archivio-checkpoint-20260623-20260726.tar.gz` (97 file), `archivio-checkpoint-20260727-20260802.tar.gz` (10 file, narrati in `WIKI.md`) e `archivio-checkpoint-20260803-20260804.tar.gz` (7 file, fusi nel consolidato).
- `.claude/WIKI.md` resta la narrazione del periodo 27/07-03/08: architettura, otto campagne, lezioni di metodo, divieti operativi e comandi di verifica.
- La cartella `.claude/checkpoints/` è in `.gitignore` alla riga 17, quindi vive solo sul filesystem: i checkpoint non entrano in nessun commit.
- Il `CLAUDE.md` di progetto è anch'esso in `.gitignore`, e `~/.claude` non è un repository git: le modifiche fatte li non sono versionate da nessuna parte.
- `PROJECT-TOD.md` e `.claude/PROMPT_CONTINUA_20260728.md` sono stati eliminati perché superati; la loro storia resta in git.
