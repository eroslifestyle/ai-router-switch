# ai-router-switch — TODO unico

**Aggiornato:** 2026-08-03 (notte) · **HEAD di riferimento:** `5ab60b7` · **Stato:** 14 voci aperte, di cui 3 di sola osservazione e 2 opzionali. Chiuse le tre incertezze della modalità qwen (`40708f6`, `130d6ad`, `f8bf84f`) e la pulizia del codice morto, con l'allarme quota GLM/Qwen collegato (`b9dbe8f`, `a76cd53`, `4dfcae8`). Suite: **188 passed, 0 errors** dalla root e **129** da `sviluppo/tests/` (misurate il 03/08 sera, dopo i 20 test nuovi della modalità qwen). Servizio `active`, **otto** porte a 200 (8787 + 8771-8778, la 8778 è nuova).

Il 03/08 questo file è stato riallineato al merge di tutte le sessioni del periodo: chiusa la voce `mix-gm` con l'evidenza del log (il test dal vivo era già passato il 31/07 e veniva trascinata come aperta), e recuperate **sei voci** che vivevano solo nei checkpoint e non erano mai arrivate qui — i tre follow-up del self-healing, le due osservazioni aperte il 01/08 e la disconnessione dei connettori Google. La narrazione completa del periodo sta in **[`.claude/WIKI.md`](WIKI.md)**.

Il 02/08 chiuso il bug «MiniMax allucina o risponde vuoto» in `mix-am`: il system prompt non arrivava mai all'esecutore, più 11 difetti misurati su shrink e telemetria (`.claude/checkpoints/CP_20260802_1743.md`). Il 01/08 chiuso lo stall `Response stalled mid-stream` di `mode anthropic`: era prompt caching morto, due bug sui `cache_control` (`.claude/checkpoints/CP_20260801_1300.md`). Il 27–28/07 campagna di 6 fix sul context rate, F1 e F8–F11 (`.claude/checkpoints/CP_20260728_0736.md`).

Questo file unifica il vecchio `TODO.md` (32 sezioni, 5 blocchi «Attivo» sparsi), `PROJECT-TOD.md` (fermo al 2026-07-18, backlog AQ nel frattempo chiuso) e le voci residue dei 97 checkpoint di sessione. Contesto completo: `.claude/checkpoints/CP_20260727_1600.md`. Il dettaglio verboso dei completati resta recuperabile con `git show fc3fbe8:.claude/TODO.md`.

## Aperti

- [x] **Settima modalità del router: `qwen` (Alibaba Cloud Model Studio). CHIUSA il 2026-08-03, ATTIVA e verificata in produzione con credenziali reali.**
  **Cosa c'è.** THINK `qwen3.8-max`, ACT `qwen3-coder-plus`, porta 8778. Backend `src/qwen_backend.py` modellato su `glm_backend.py` (stesso endpoint Anthropic-compatible di z.ai), servizi DashScope separati in `src/qwen_generative.py`. CLI `qwen-code`, `qwen-web`, `qwen-setup`. Account `ws-XXXXXXXXXXXXXXXX` (Singapore).
  **Verificato dal vivo sulla porta 8778:** claude-opus-5 → qwen3.8-max risponde PONG; claude-haiku → qwen3-coder-plus risponde PING; streaming SSE, tool calling (stop_reason=tool_use con argomenti corretti), thinking e cache_control tutti OK; /v1/embeddings → text-embedding-v4 con vettore da 1024 dimensioni; /v1/images/generations → URL PNG realmente generato; qwen-web → risposta aggiornata con 16 fonti citabili. 13 modelli su 13 testati rispondono 200.
  **Quattro assunzioni smentite dalla misura.** (1) L'host DashScope NON è dashscope-intl: il CSV delle credenziali della console dichiara l'host DEDICATO del workspace. (2) I servizi nativi vogliono `Authorization: Bearer`, non `x-api-key`. (3) Il MCP WebSearch di Bailian NON esiste in regione internazionale (404 su entrambi gli host): registrazione resa opt-in. (4) La web search NON restituisce fonti su /compatible-mode/v1/chat/completions (277 eventi ispezionati, zero occorrenze): solo su DashScope nativo in SSE.
  **Due scoperte non documentate.** Il gateway risponde 413 RequestTooLarge sui BYTE del corpo, PRIMA di valutare il contesto, senza dichiarare il limite: 4,8 MB passano, 6,7 MB no (guardrail `QWEN_MAX_BODY_BYTES` a 4 MB). E qwen3-coder-plus ha accettato 1.000.009 token di input: il milione è misurato.
  Vault: `progetti/ai-router-switch/modalita-qwen-attivazione-produzione-20260803.md`. Checkpoint: `.claude/checkpoints/CP_20260803_2153.md`.

- [x] **Il relay non sapeva gestire gli errori di GLM e Qwen; un 429 persistente degradava a 502. CHIUSA il 2026-08-03.**
  **Causa:** entrambi i backend ritornavano una `aiohttp.web.Response` sui percorsi d'errore. Con passthrough=True finisce in `StreamingRelay.relay()`, che ci chiama `.release()` e `.content.iter_any()` sopra: l'AttributeError sostituiva il messaggio vero con un 502 opaco. Sul router vivo si leggeva «'Response' object has no attribute 'release'» invece di «qwen key missing».
  **Fix:** la soluzione esisteva già nel progetto in DUE copie non condivise (`_SyntheticResponse` in forward_minimax, `_PreReadResponse` in forward_anthropic), estratta in `src/synthetic_response.py`. Due requisiti non estetici: `release()` SINCRONO (il relay lo chiama senza await) e headers `CIMultiDict` (il relay legge Content-Encoding e content-type nello stesso flusso). Il 429 ora resta 429 e conserva il Retry-After.
  **Da non ripetere:** asserire il numero di tentativi invece dell'esito. In passthrough il secondo tentativo non avviene mai — il cooldown del limiter fa scattare RateLimitExhausted già in acquire — e riscrivere il test sull'esito ha rivelato che il ramo qwen non forniva alcun Retry-After.

- [x] **`test_glm_modes.sh` falliva sempre da luglio (7 pass / 5 fail) e `routestats` aveva tre id di modalità obsoleti. CHIUSA il 2026-08-03, ora 11 pass / 0 fail.**
  **Cause, tutte preesistenti**, verificate rieseguendo gli stessi controlli sulla versione recuperata con `git show HEAD:`: usava `glm-minimax` e `anthropic-glm`, rinominati mix-gm/mix-ag il 2026-07-26 e da allora scartati da `AIROUTER_PORT_MODE_JSON` che filtra su VALID_MODES, quindi due porte non venivano mai aperte; asseriva che `heuristic_tier` classificasse per contenuto mentre classifica per DIMENSIONE del body; faceva monkeypatch di `should_block_glm_model` su `glm_backend` mentre `apply_peak_cap` lo risolve con import lazy da `peak_scheduler`. In `routestats` gli stessi tre id obsoleti rendevano lo switch del pannello rotto per metà delle modalità, in entrambe le direzioni.
  **Aggiunto** un esito `skip` che distingue un 429 upstream da un difetto del router: mix-ag manda il THINK ad Anthropic e un 429 org-level non dice nulla sul router.
  **Da non ripetere:** lasciare che un test fallisca sempre. Un test che mente nasconde le regressioni vere.

- [x] **Misurare i context window di tutti i modelli.** CHIUSA il 2026-08-03 per tutti e 14 i modelli. La tecnica prevista (ricerca per accettazione col probe) è stata superata da una tecnica a costo zero: inviare un payload sopra ogni limite noto (1.500.000 token con filler a 2 caratteri per token, 2,86 MB) e leggere il messaggio del 400, che dichiara "Range of input length should be [1, N]". Una richiesta rifiutata non fa pagare l'input. **Due valori erano sovrastime gravi:** qwen3-max era 1.000.000 ed è 258.048; qwen3-coder-next era 1.000.000 ed è 204.800. **Cinque erano sottostime fino a 3,75 volte:** i flash erano 262.144 e sono 983.616; coder-flash 997.952; qwen-flash 1.000.000; vl-plus era 131.072 ed è 260.096. qwen3-coder-plus dichiara 1.048.576, confermando 1.000.009 token realmente accettati. **Nota tecnica:** probe_qwen.py aveva un secondo artefatto oltre a `parole[:100]`: costruiva il filler come `"word "` moltiplicato per `mid/4`, mentre `"word "` vale 1 token ogni 5 caratteri, quindi inviava un quarto dei token dichiarati; col tetto a 1.100.000 non poteva superare ~275.000 token reali e non avrebbe mai potuto misurare un limite da un milione. Ora il probe tenta prima la sonda a costo zero e legge `usage.input_tokens` come verità. **Costo della misura:** ~670.000 token di input, tutti su modelli flash, di cui 524.000 spesi nell'unico esperimento per accettazione che la tecnica a costo zero ha poi reso superfluo. Commit 40708f6.

- [x] **Verificare i path DashScope di video, ASR e musica.** CHIUSA il 2026-08-03, commit 130d6ad. Il metodo previsto (POST con body vuoto, 400 = valido, 404 = sbagliato) NON funziona su questo gateway: body vuoto restituisce sempre 400 BadRequest.EmptyModel, anche su path inventati. Con un model valido nel body la misura discrimina: path sbagliato → InvalidParameter "url error, please check url", path corretto → prosegue. Esiti: QWEN_PATH_ASR era SBAGLIATO (multimodal-generation invece di /api/v1/services/audio/asr/transcription), QWEN_PATH_VIDEO era già corretto; entrambi sono servizi a task asincrono e senza l'header X-DashScope-Async rispondevano 403 sistematicamente, ora _forward_dashscope gestisce async_task; il modello fun-music-v1 NON esiste sull'account (risponde 404 "Model not exist" identico alla controprova), quindi la rotta musica non può funzionare e il suo path resta non determinabile (il gateway valida il modello prima del path); rimossa doppia assegnazione morta di QWEN_PATH_IMAGE e QWEN_PATH_TTS.
- [x] **Reperire i limiti RPM/TPM reali di DashScope.** CHIUSA il 2026-08-03, commit f8bf84f. I valori ufficiali letti da https://www.alibabacloud.com/help/en/model-studio/rate-limit per la regione ap-southeast-1 Singapore (quella dell'account). I placeholder erano fino a 50 volte sotto il reale: qwen3.7-flash 300 RPM invece di 15.000, qwen3.7-max 60 invece di 600. Il dizionario passa da 5 a 14 voci; i modelli in uso sono qwen3.8-max (600 RPM, 1.000.000 TPM) e qwen3-coder-plus (2.400 RPM, 2.000.000 TPM). I limiti variano per regione e le versioni datate hanno limiti molto più bassi (tipicamente 60 RPM). Il rate limiting non si applica alle chiamate via Batch API. Il gateway NON espone header di quota, verificato ispezionando le risposte, quindi i limiti non sono osservabili a runtime e vanno mantenuti allineati manualmente.
- [x] **Rotte generative GLM: rimosso il codice morto.** CHIUSA il 2026-08-03, commit b9dbe8f, a76cd53 e 4dfcae8. route_glm_request, forward_glm_image e forward_glm_video erano orfani (nessun chiamante verificato con grep, grafo delle chiamate e assenza di chiamate dinamiche); rimossi insieme alle costanti GLM_IMAGE_ENDPOINT e GLM_VIDEO_ENDPOINT definite due volte, la stessa doppia assegnazione morta trovata in qwen_generative. In modalità glm le immagini continuano ad andare a MiniMax, comportamento invariato. Lo stesso giro ha diviso altri orfani in due gruppi: difetti di aggancio (glm_alert collegato a classify_429_glm, qwen_alert collegato al 429 e con throttle di 300 secondi che mancava, rotta ASR di qwen aggiunta a _GEN_ROUTES) e residui ordinari rimossi (10 pezzi: _escalate_anthropic con _try_shrink_body_haiku, _synthetic_context_exceed, is_glm_body_too_large, is_qwen_body_too_large, _fire_and_forget con _background_tasks, _alert_log con ALERT_LOG, _force_no_stream, _count_tokens_stub, src/hhem_gate.py). Bilancio: 374 righe rimosse, 68 aggiunte, 188 passed, test_glm_modes.sh 11 PASS 0 FAIL.
- [x] **apply_peak_cap collegata, declassamento GLM in fascia peak attivo.** CHIUSA il 2026-08-04. La funzione è ora chiamata dal ramo GLM del proxy (src/ai-router-proxy.py, subito dopo il calcolo del modello da role_routing); in fascia peak 14-18 Asia/Shanghai glm-5.2 e glm-5-turbo vengono declassati a glm-4.7 e il declassamento è loggato con prefisso "GLM peak-cap". Durante l'indagine emerge che la funzione accettava solo TIER KEY mentre il proxy ha in mano un NOME MODELLO: senza la mappa inversa GLM_TIER_FOR_MODEL collegarla sarebbe stato un no-op SILENZIOSO. Sono orfane anche classify_tier e heuristic_tier perché il tiering per complessità non esiste più dal refactoring del 2026-07-25: i modelli GLM in circolazione sono due e li decide il ruolo (glm-5.2 THINK, glm-4.7 ACT). Corretti quattro documenti che davano per attivo un comportamento diverso (README.md, docs/MANUAL.md, docs/MANUAL.en.md, docs/index.md); tre test coprono il caso, incluso uno di guardia anti-scollegamento che fallisce se il proxy smette di chiamare la funzione.
- [x] **Residui THINK/VERIFY e campionamento token AQ-8.** CHIUSA il 2026-08-04, commit 095782e e 9a2243b. Nessuno dei due era il residuo isolato che il TODO registrava. build_glm_verify_body NON era orfana: la usava sviluppo/tests/test_mixgm_verify_retry.sh. Ma quel test era gia rotto, PASS=4 FAIL=1, perche il suo caso [3] cercava _build_minimax_act_body_retry, assente dal proxy, e i casi [2] [4] [5] erano Python auto-referenziale che simulava la logica dentro il test stesso. Rimossi insieme la funzione, il test, build_glm_think_body, le costanti GLM_THINK_VERIFY_MODEL e GLM_THINK_TIMEOUT_SEC e, in cascata, l'helper _extract_text. count_tokens_real era la punta di un sotto-sistema intero mai completato: count_tokens, che avrebbe dovuto consumarla, era importata dal proxy ma mai chiamata, con una docstring che prometteva "conta token reali" e un parametro upstream_url mai usato; il gemello _count_tokens_real in context_manager non era la versione viva ma un altro stub, con return None incondizionato e la guardia _TOKEN_STUB_AVAILABLE cablata a False. Rimossi tutti e tre, piu _token_count_cache, CACHE_TTL_SEC, gli import time/Dict/Tuple e i due import morti nel proxy. La stima resta viva: estimate_tokens_body, bytes_per_token, _estimate_tokens. Bilancio della sessione: 299 righe rimosse, 168 aggiunte, 191 passed, test_glm_modes.sh 11 PASS 0 FAIL.
- [x] **Orfane GLM residue.** CHIUSA il 2026-08-04, commit 213309e. Rimossa la coppia classify_tier e heuristic_tier, senza chiamanti dal refactoring del 2026-07-25; con loro cade has_multimodal_content di cui classify_tier era l'unico chiamante, e la costante _ANTHROPIC_BLOCKED che aveva la sola definizione. Il caso [2] di sviluppo/tests/test_glm_modes.sh copriva heuristic_tier e avrebbe fallito con AttributeError: tolte quelle tre assert, tenute tutte quelle su apply_peak_cap. Stesso schema di build_glm_verify_body, funzione e test valutati insieme, ma con esito opposto: lì il test era già rotto e auto-referenziale, qui era verde e andava solo potato. Restano vive le costanti GLM_TIER_* e la mappa GLM_MODEL_FOR_TIER, perché le usa apply_peak_cap che accetta indifferentemente una tier key o un nome modello. Aggiornata la docstring del modulo che descriveva tre tier e la voce R3-#4 su classify_tier. **Nota di metodo:** la scansione automatica degli orfani produce falsi positivi e va sempre controprovata; backend_from_final e detect_foreign_tool_use sembravano orfane ma sono usate da streaming_relay.py nella ROOT del progetto.
- [x] **`mix-am`: «MiniMax genera contenuti allucinati o vuoti». CHIUSA il 2026-08-02, verificata end-to-end sul router vivo.**
  **Causa:** il **system prompt non arrivava MAI** all'esecutore MiniMax. `minimax_body._inject_system_as_message` spostava il campo top-level `system` dentro `messages` come `{"role":"system"}`; subito dopo, in `forward_minimax` con `AIROUTER_TRANSITION_FILTERS=1` (attivo in produzione, letto da `/proc/<pid>/environ`), `_repair_message_sequence` applicava `[m for m in messages if m.get("role") != "system"]` e cancellava proprio quel messaggio. Due trasformazioni singolarmente sensate che si annullavano. Sui body grandi spariva con esso anche il riassunto del contesto, che `context_rewrite` mette nel `system`.
  **Prova A/B** con system «rispondi SEMPRE e SOLO BANANA»: MiniMax diretto → `BANANA`; via router → si presentava («Ciao! Mi chiamo MiniMax-M2.7»), **identico al caso senza system**; dopo il fix → `BANANA`. **Prova contesto:** body 1135KB → `input_tokens` **736**, risposta «CODICE-ALFA: (non fornito)»; dopo → **86.944**, risposta corretta.
  **Fix:** rimosse `_inject_system_as_message` e `_system_to_text`; il `system` resta top-level. Verificato che `api.minimaxi.chat/anthropic` lo onora anche come **lista di blocchi con `cache_control`**: la conversione era inutile oltre che dannosa.
  **Altri 10 difetti misurati e corretti.** (1) Tutti e tre i percorsi di shrink (`pipeline_minimax._try_shrink_body`, `pipeline_anthropic._try_shrink_body_haiku`, `context_rewrite._rewrite_impl`) buttavano il riassunto nel fallback — quest'ultimo sceglieva `min(candidates, key=len)`, cioè il candidato più povero, in silenzio. (2) `_try_shrink_body_haiku` assegnava `shrunk["messages"]` **prima** di `_repair_message_sequence` senza riassegnare: la riparazione non aveva effetto. (3) `system` lista serializzato con `json.dumps` grezzo nei path di shrink. (4) `streaming_relay._acc_limit=16384` leggeva solo i primi 16KB → `outcome="empty"` **falso** (il 91% degli empty MiniMax aveva >4000 token, mediana 9982): aggiunto buffer di coda + flag `measure_partial`, `classify_outcome` ora dice `unknown`. (5) Il ramo **non-streaming** del relay non contava i blocchi → ogni risposta JSON riuscita risultava `empty`. (6) `trim_smart._smart_sample_middle` **ignorava il budget** sui messaggi pesanti (300 messaggi → 549.418 caratteri sia con budget 560.000 sia con 8.000): i loop iterativi appena introdotti non convergevano proprio nel caso peggiore. (7) `context_rewrite` passava il budget in **token** dove serve in **caratteri** → riassunto 4x più piccolo, 52k dei ~196k token consentiti; con `bytes_per_token(model)` sale a 86.944 (43% della finestra). (8) `final_override` etichettava `MiniMax-M3` richieste servite da M2.7, 834 in 7 giorni. (9) `startswith("MiniMax")` case-sensitive sugli alias minuscoli. (10) `debug_catalog` non sanificava gli snippet: byte gzip → **155 NUL** in `BUG-CATALOG.md`, che git classificava **binario**. Rimosso inoltre `src/summarizer.py`, codice morto che costruiva anch'esso un `role="system"`.
  **Da non ripetere:** «MiniMax non supporta `role=system`» è **falso** (accetta entrambi i formati); `outcome=empty` della telemetria MiniMax **non** prova una risposta vuota senza controllare `measure_partial`; `minimax-m2.7-hs` **non** è un modello coding distinto (l'upstream lo risolve da sé a M3, il coding si chiede come `MiniMax-M2.7`); un calo di `input_tokens` dopo un restart va sommato a `cache_read` prima di gridare alla regressione (136+74811 = 74947, invariato).
  **Limite noto accettato:** si usa il **43%** della finestra, non il 75% teorico, per via di `TRUNCATE_MAX_LEN=1800`; alzarlo richiede una misura della qualità del riassunto. Commit `8cc2922`, `77b311c`, `e757bf3`. Vault: `progetti/ai-router-switch/system-prompt-perso-esecutore-minimax-20260802.md`.

- [x] **Stall `API Error: Response stalled mid-stream` in `mode anthropic`. CHIUSA il 2026-08-01, verificata sul traffico vivo.**
  **Causa:** non era il router a troncare gli stream, era il prompt caching morto. `cache_creation=0` con `input` 151k–226k riprocessati a ogni turno → generazione a **un chunk ogni 12 secondi** (27 chunk in 5m30s) → il client chiudeva: `relay loop ERROR ... [client_closing=True upstream_eof=False]`. Le sessioni sane in parallelo avevano `input=2` e chiudevano in 30–40 s.
  **Due bug distinti sui `cache_control`.** (1) `aeccaaf` — `filter_tools_for_backend` ([`src/tool_isolation.py`](../src/tool_isolation.py)) rimuoveva i tool stranieri **senza trasferire il breakpoint** che Claude Code mette sull'ultimo tool: `stripped=['mcp__zai__web_search_prime'] kept=57/58`, **3263 occorrenze**. Ora il `cache_control` passa all'ultimo tool rimasto, e il tool straniero resta rimosso (isolamento intatto, ultimo strip alle 12:54:02). (2) `f158d50`, **decisivo** — la coda della conversazione restava senza breakpoint. Reso visibile da `ae8a74d`, che logga il conteggio per sezione: `cache: OK bp=s2/m1/t0 read=56982 creation=0 input=226748` — 3 slot su 4, e l'unico sui messaggi era su un turno *vecchio*. Nuovo [`src/cache_optimizer.py`](../src/cache_optimizer.py), agganciato in `src/forward_anthropic.py` in due punti dopo `sanitize_server_tool_ids`.
  **Risultato misurato** (restart 12:46:30): `input` non cachato **da 226.748 a 2** (mediana su 32 richieste), `creation` da 0 fisso a 24k–36k, `cache_read` in crescita monotona fino a **320.924**, velocità stream **da 0,08 a 9,5 chunk/s**, **0** stall patologici. Suite **168 passed**.
  **Errore di analisi da non ripetere:** lo strip dei tool **non** è esclusivo delle modalità pure — `src/forward_anthropic.py` lo applica anche alla leg Anthropic delle miste (righe 221 e 327). La discriminante è se il tool rimosso *portava* il breakpoint, cioè l'ordine dei tool nel workspace: per questo colpiva una chat sola.
  **Residuo (solo osservazione):** la firma di salute è `input=2` nel log `cache: OK bp=...`; se ricompare `creation=0` con input a sei cifre il caching è di nuovo morto. `client_closing=True` = client sparito, `False` = colpa del router. Limite latente mai osservato scattare: `sock_read=120` sugli stream (le non-streaming sono a 600 s dal fix `7c84535`) — non toccato per assenza di evidenza.

- [x] **Massimizzare l'esecuzione su MiniMax in `mix-gm` — enforce_hierarchy deny mode-aware. CHIUSA il 2026-08-03 con l'evidenza del log: il test dal vivo era già avvenuto il 31/07.**
  **Chiusura (2026-08-03).** La voce risultava aperta per trascinamento: `CP_20260731_1238.md` dichiarava il test dal vivo passato, ma `CP_20260802_1743.md` la riportava fra i to-do «preesistenti» senza riverificarla. Conteggio su `~/.claude/m3/hierarchy-violations.jsonl` (13.480 entry totali): **13 deny `mix-gm-delegate-to-executor`**, dal 2026-07-31T08:11:04 al 2026-07-31T20:05:32; più **55 `mix-am-delegate-to-executor`** fino al 2026-08-02T21:46:59 e **2 `minimax-delegate-to-executor`**. L'estensione a `{mix-gm, mix-am, minimax}`, che la voce elencava come opzionale, risulta quindi applicata e attiva su tutte e tre le modalità.
  **Variazione del 2026-08-02, su richiesta esplicita dell'utente:** l'intercettazione delle redirezioni Bash è stata declassata da blocco ad audit. `_check_bash_redirect` (`~/.claude/hooks/enforce_hierarchy.py:243`) registra e lascia passare — riga 275, reason `{mode}-bash-redirect-audit`. Il deny su Edit/Write/MultiEdit resta invariato (`DENY_CODING_MODES` alla riga 25).
  **Da non ripetere:** riportare una voce come aperta perché la si trova aperta nel checkpoint precedente. Prima di trascinarla, controllare se il bersaglio esiste ancora — qui bastava contare le entry del log.
  **Diagnosi.** mix-gm delega all'esecutore solo nel 18% dei casi (GLM 82% / MiniMax 18% su 905 richieste in `ai-router.log`), contro il 36% di mix-am (Anthropic 64% / MiniMax 36% su 7.832). Nessun bug nel proxy: tabella `src/role_routing.py:55-56` e chain `~/.claude/lib/router_chain_dispatcher.py:242-253` erano corrette (`mix-gm` THINK=glm-5.2, ACT=MiniMax-M2.7). Il buco era l'enforcement: la Regola Assoluta 2026-07-02 vincolava solo Anthropic → GLM-5.2 come THINK scriveva codice direttamente; e `~/.claude/hooks/enforce_hierarchy.py` era audit-only (`permissionDecision: allow` fisso dal fix 2026-07-14).
  **Fix (verificato 5/5 test).** `enforce_hierarchy.py` ora è mode-aware: set `DENY_CODING_MODES = {"mix-gm"}` → Edit/Write/MultiEdit su codice di progetto (>15 righe, non `~/.claude`) restituiscono `permissionDecision: deny` con reason che ordina `m3-code "<spec>" > file`. Esenzioni invariate (micro-edit ≤15, `~/.claude`, `HIERARCHY_ALLOW=1`, marker 900s). `~/.claude/CLAUDE.md` regola estesa a GLM-5.2 come THINK in mix-gm. `deny` ≠ `exit 2`: non rompe il prompt di autorizzazione.
  **Dato cruciale.** `m3-code` chiama MiniMax DIRETTAMENTE (out-of-band, non via :8787): le deleghe NON appaiono nel log proxy. Indicatore di successo = riduzione Edit/Write di progetto in `~/.claude/m3/hierarchy-violations.jsonl` + invocazioni `m3-code`, NON i conteggi di `ai-router.log`.
  **Prossimo step.** Test dal vivo in sessione mix-gm: GLM deve ricevere deny e delegare a `m3-code`. **Expected outcome:** entry `action:"denied"` in `hierarchy-violations.jsonl` su Edit/Write di progetto. **(opzionale)** estendere `DENY_CODING_MODES` a `{mix-gm, mix-am, minimax}` su conferma utente. Dettaglio: vault `sistemi/enforce-hierarchy-deny-mix-gm-massimizza-minimax-20260731`, checkpoint `CP_20260731_1238.md`.

- [x] **Valutare la Models API di Anthropic per sostituire `src/model_context_map.py`. CHIUSA il 2026-07-28: sostituzione a runtime scartata, API adottata come controllo di coerenza offline; trovata una sottostima di 5x su `claude-sonnet-4-5`.**
  **Misura.** `GET https://api.anthropic.com/v1/models?limit=100` risponde HTTP 200 con il token OAuth di Claude Code (header `authorization: Bearer` più `anthropic-beta: oauth-2025-04-20`). Restituisce 11 modelli, `has_more` false, e ogni voce espone sia `max_input_tokens` sia `max_tokens` (il massimo di output). Confronto col traffico reale del sidecar `router-usage.jsonl`, su **112.244 richieste Anthropic**: mappa statica e API concordano sul **99,97%**. Divergenti: **9 richieste, lo 0,01%**. Su modelli che l'API non copre affatto: **18 richieste, lo 0,02%**.
  **Decisione.** **NON sostituire a runtime.** La Models API entra come controllo di coerenza offline: nuovo script [`sviluppo/tools/check_model_context.py`](../sviluppo/tools/check_model_context.py). La mappa statica resta l'unica fonte usata dal proxy. Nessuna seconda tabella di context limit è stata creata: lo script non contiene nemmeno un valore, li legge tutti dalla mappa e dalla risposta HTTP.
  **Perché non a runtime.** Tre motivi, in ordine di peso. Primo: il guadagno correttivo immediato è lo 0,01% del traffico, mentre il costo è una dipendenza di rete con cache e fallback dentro l'hot path di un componente critico. Secondo: il catalogo dell'account non contiene 9 chiavi Anthropic della mappa, che con una sostituzione secca cadrebbero sul default di 200.000 e verrebbero compresse a 160.000 dal gate; fra queste `claude-sonnet-4-7` (1.000.000, con traffico reale: 5 richieste, più 1 su `claude-sonnet-4-7-20250620`) e `claude-sonnet-4-8` (1.000.000, oggi senza traffico). Terzo: se l'API fosse irraggiungibile al boot, l'unico fallback possibile sarebbe di nuovo una tabella statica, cioè la doppia verità che si voleva evitare.
  **Cosa resta a mano.** 13 chiavi non Anthropic, che l'API non copre per definizione: `MiniMax-M3`, `MiniMax-M2.7`, `MiniMax-M2.5`, `MiniMax-M2`, `MiniMax-M3.5`, `MiniMax-Haiku`, `glm-5.2`, `glm-5-turbo`, `glm-4.7`, `glm-4.6v`, `glm-4v`, `glm-4`, `glm-5V-Turbo`. Più 9 chiavi Anthropic fuori dal catalogo dell'account, che lo script segnala come avviso atteso e non come errore: `opus`, `sonnet`, `haiku`, `sonnet-4-7`, `claude-sonnet-4-7`, `sonnet-4-8`, `claude-sonnet-4-8`, `claude-mythos-5`, `claude-mythos-preview`.
  **Correzione trovata.** Una sola divergenza reale: `claude-sonnet-4-5` valeva 200.000 nella mappa mentre l'id di catalogo `claude-sonnet-4-5-20250929` riporta 1.000.000, una sottostima di 5 volte. Il vecchio valore veniva dalla sezione SPEC non verificata. Allineato a 1.000.000 con fonte e data nel commento.
  **Verifica.** Lo script esce **0** quando mappa e catalogo concordano, **1** quando c'è divergenza, **2** quando manca il token o la rete non risponde — un controllo eseguito offline non deve risultare rosso. Sei controprove eseguite: rileva un modello nuovo non mappato, rileva un valore divergente, non produce falsi positivi sul catalogo reale, ignora le chiavi MiniMax e GLM, riconosce gli alias senza prefisso `claude-`, e non aggancia un alias generico a una variante più recente. La controprova sulle chiavi ignorate ha fatto emergere un difetto nello script stesso: `is_anthropic_key` cercava solo la sottostringa `claude` e lasciava fuori dal controllo `sonnet-4-6`, `sonnet-4-7` e `sonnet-4-8`, che sono Anthropic a tutti gli effetti. Corretto.
  **Non aggiunto alla suite pytest**: la renderebbe dipendente dalla rete e dal token OAuth. Va eseguito a mano o da CI: `python3 sviluppo/tools/check_model_context.py`. Suite invariata: **120 passed, 0 errors**.

- [x] **502 «Timeout on reading data from socket» sulle richieste non-streaming lunghe. CHIUSA il 2026-07-28, verificata sul live.**
  **Causa:** la sessione condivisa ([`src/ai-router-proxy.py:891`](../src/ai-router-proxy.py#L891)) impone `sock_read=120` a ogni richiesta. Su una risposta **non-streaming** l'upstream non manda un byte finché la generazione non è conclusa: quei 120 s smettono di proteggere dagli stall e diventano **un tetto sulla durata della generazione**. 59 casi in `ai-router.log` fra il 26 e il 28/07, tutti su `minimax passthrough`, ~43 su traffico reale (fasce serali del 26–27, mattina del 28).
  **Non era un difetto di MiniMax:** `forward_anthropic` non passava alcun timeout per-request e `glm_backend` usava `total=120`, il più stretto dei tre. Non si manifestava perché quei client usano sempre lo streaming. Il fix `4a256ce` del 19/07 aveva coperto solo il ramo `act_timeout_sec`, non il passthrough.
  **Fix (`7c84535`):** `NON_STREAM_SOCK_READ_SEC` (env `AIROUTER_NON_STREAM_SOCK_READ_SEC`, default 600 s) + helper `upstream_timeout_for(body)`, applicato a MiniMax (passthrough + endpoint generativi), Anthropic (3 call site) e GLM (ramo non-passthrough). Lo streaming continua a ereditare i 120 s, dove servono davvero. In `glm_backend` l'import è **lazy e senza `except`**: un fallback muto lì ha già causato la regressione del 2026-07-25.
  **Telemetria:** il 502 del passthrough finiva solo in `ai-router.log`, mai nel sidecar (59 contro 0) — ora registrato, altrimenti il fix non sarebbe verificabile sul traffico reale.
  **Verifica live (dopo restart, procedura completa):** la richiesta non-streaming con `max_tokens=32000` che prima moriva a 120,4 s ora ritorna **HTTP 200 in 126,9 s**, `end_turn`, `thinking` + `text` completi. Suite **110 → 120**. Controprova eseguita: disattivando l'override il test e2e fallisce, quindi è discriminante.

- [x] **4 test non venivano mai eseguiti, e la suite diceva comunque «106 passed». CHIUSA il 2026-07-28 con la strada (a): i test sono entrati in suite.**
  **Strada scelta e perché:** (a), non (b). I 4 test si sono rivelati **verdi già come script standalone**, quindi la (b) — rinominarli fuori dal pattern `test_*` — avrebbe congelato fuori dalla copertura proprio il gate di contesto e il TTFB di `mix-gm`, cioè l'area della campagna context rate appena chiusa. Farli entrare in suite costa tre file toccati e restituisce quattro test veri.
  **Risultato misurato:** `python3 -m pytest -q` → **110 passed, 0 errors** (era 106 passed + 4 errors); `python3 -m pytest sviluppo/tests/ -q` → **62 passed, 0 errors** (era 58 + 4). I 4 test risultano `PASSED` in `-v`, non `SKIPPED`. Tre run consecutivi identici. Gli script standalone (`python3 sviluppo/tests/test_*.py`) continuano a funzionare: i runner `main()` non sono stati rimossi.
  **Cosa è servito, oltre alla fixture.** Tre problemi distinti, due dei quali non erano nella diagnosi iniziale:
  1. *Fixture assente* — nuovo `sviluppo/tests/conftest.py` con `@pytest_asyncio.fixture h` che prende la classe da `request.module.Harness` (i due harness sono diversi: upstream JSON contro upstream SSE lento) e garantisce `stop()` nel `finally`.
  2. *Marker troppo tardi* — `pytest-asyncio` 1.4.0 gira in **strict** (nel repo non c'è `pytest.ini`/`pyproject.toml`/`setup.cfg`). Aggiungere `pytest.mark.asyncio` da un hook `pytest_collection_modifyitems` **non funziona**: il plugin ha già deciso, e i test falliscono con «async def functions are not natively supported». Serve `pytestmark = pytest.mark.asyncio` **a livello di modulo** nei due file. Scartato `asyncio_mode = auto`: avrebbe imposto una rootdir e un auto-wrap a tutta la suite.
  3. *Moduli cachati* — ogni harness redirige gli upstream su una porta effimera via env **prima** di importare il proxy, ma `router_constants` risolve quegli indirizzi una volta sola all'import e resta in `sys.modules`: nella stessa sessione il secondo file ereditava la porta del fake server del primo, ormai chiuso, e prendeva **502**. Il conftest scarica i moduli di `src/` prima di ogni `start()` e dopo ogni `stop()`. **Verificato per controprova:** disattivando entrambi i call site, 2 test su 4 tornano rossi.
  **Danno collaterale trovato e corretto:** `sviluppo/tests/test_mix_anthropic_retry.py:39` usava `asyncio.get_event_loop().run_until_complete()`. Da solo passa, ma appena un test asyncio gira nella stessa sessione il plugin azzera il loop globale e quei **6 test esplodono** con `RuntimeError: There is no current event loop` — regressione che si è manifestata al primo giro di verifica. Sostituito con `asyncio.run()`, che è anche l'unica forma che sopravvive a Python 3.14. Il pattern era presente in un solo punto dell'intera suite (verificato con grep su `sviluppo/tests/` e `tests/`).

- [x] **Misurare l'effetto reale dei fix sul context rate (F1, F8–F11). CHIUSA il 2026-07-28: la baseline era un artefatto di misura, non una perdita di token.**
  **Misura richiesta:** 4.779 richieste ≥20k senza cache / 187.348.464 token sui 7 giorni, contro la baseline di 5.215 / 201.283.024. Calo grezzo −8,4%, ma **il confronto non ha significato**: 5 dei 7 giorni sono ancora traffico misurato con la telemetria rotta.
  **Root cause della baseline:** `9fdde3f` (26/07 **10:10**) — «decomprimi il buffer prima di estrarre usage e cache tokens». Con `auto_decompress=False` il relay leggeva byte gzip come utf-8, non trovava mai `message_start`, quindi loggava `cache_read`/`cache_creation` **sempre a 0** e stimava `input_tokens` con `chars//4`. Il commit lo dichiara: «solo telemetria, nessun impatto sulle risposte». **La baseline è stata scritta il 27/07 su dati già invalidati il 26/07.**
  **Prova (taglio al minuto del fix):** prima di `9fdde3f` 5.213/5.312 big senza cache = **98,1%**; dopo, 2/387 = **0,5%**. Il 5.213 pre-fix coincide col 5.215 della baseline. Correlazione oraria: restart 26/07 10:09:01, il nocache passa da 100% (ore 06–09) a 54,3% (ore 10) a 0% (dalle 11 in poi, per due giorni). Il 28/07: 38 richieste big, **0 senza cache**, hit rate 80,2%.
  **Effetto dei 6 fix su questa metrica: non misurabile** — era già a zero 36 ore prima che F1 (`f05ea98`, 27/07 23:05) fosse deployato. Non significa che F1 fosse inutile: il bersaglio era reale ma di **due ordini di grandezza più piccolo**, cioè i **121 `ctx: proactive rewrite`** loggati il 23/24/26 luglio, ognuno dei quali distruggeva i `cache_control`. Il 28/07, dopo `49c26a3`, i rewrite sono **0**.
  **Nessuna causa da cercare altrove**, e nessuna estensione della diagnostica del relay: la perdita apparente è spiegata e chiusa. Da non ripetere: citare 5.215 / 201M come token persi.

- [x] **G4 — non mutilare le richieste di compattazione. CHIUSA il 2026-07-27: nessuna azione sul proxy.**
  **Marker trovato** (dal binario `~/.local/lib/node_modules/@anthropic-ai/claude-code/bin/claude.exe`, non dai log): il messaggio user del turno di compattazione inizia sempre con `CRITICAL: Respond with TEXT ONLY. Do NOT call any tools.` — funzioni `WAo` (compact pieno, costante `MPy`) e `Usd` (varianti `up_to`/`from`, `$Py`/`OPy`), coda `Bsd`, costruito via `zr({content})` come messaggio user. È version-dependent: cambia col bundle del CLI.
  **La vecchia condizione di sblocco era inefficace.** Il 27/07 tre `/compact` manuali reali hanno prodotto **zero** entry `ctx_gate`: `_should_record` ([`src/ai-router-proxy.py:445`](../src/ai-router-proxy.py#L445)) richiede `action in (compact,error)` oppure `pct >= 0.30`, e su provider anthropic 218.349 token = 21,8%. Un compact manuale a contesto basso non passerà **mai** da quel percorso.
  **Esito della correlazione** (`sviluppo/tools/compact_correlate.py`, commit `61f25f9`): 3 INTATTO (27/07, tutti `mix-am` → provider `anthropic`, safe limit 800.000), 2 non applicabili (17/07: modalità `mixed` pre-refactor-tunnel, 100% del traffico riscritto su `MiniMax-M3` dalle pipeline server-side poi rimosse in `99dcc0d`; né il gate osservatore né il rewrite attuale esistevano). **Zero mutilazioni sul codice di oggi.**
  **Rischio residuo — RIDOTTO dai fix del 28/07 (`2f7f0cc`, `49c26a3`).** La vecchia analisi assumeva `get_safe_input_limit("MiniMax-M2.7")` = 160.000 e **MiniMax-M3 = 200.000**. Dalla doc ufficiale M3 ha **1.000.000** di contesto e il buffer ora riserva il `max_tokens` reale: in `minimax` il THINK è M3 con safe 968.000, quindi i 3 preTokens citati (170641, 209148, 218349) **non attivano più il rewrite**. Resta la sola leg ACT su M2.7 (safe 172.800), dove quei valori scatterebbero ancora. **Riaprire solo se** un compact reale avviene sulla leg ACT di `minimax`/`mix-gm`: allora rieseguire il correlatore.

- [ ] **Triage dei finding codeburn — scelto `mcp-low-coverage`, ma si misura prima di disattivare.**
  **Contesto.** Audit codeburn del 2026-07-27, sola lettura, 30 giorni: health **F 20/100**, 855 sessioni, risparmio potenziale dichiarato 41,3% pari a 1,98 miliardi di token. I tre finding principali: `low-worth-sessions` 923M token, `context-heavy-sessions` 674M, `mcp-low-coverage` 315M.
  **Scelta.** Il primo finding su cui intervenire è `mcp-low-coverage`, nonostante gli altri due abbiano numeri assoluti più alti.
  **Perché questo per primo.** È l'unico dei tre con una leva di configurazione deterministica e reversibile: un server MCP è caricato oppure no, e i suoi schemi pesano su ogni richiesta. Gli altri due non lo sono. `context-heavy-sessions` (674M) è già stato attaccato dalla campagna context rate del 27–28 luglio (F1, F8–F11) e dal gate di contesto: il margine residuo è più piccolo di quanto il numero suggerisca. `low-worth-sessions` (923M) è il più grande ma non ha una leva tecnica singola: richiede un giudizio su cosa sia una sessione a basso valore, quindi un cambio di abitudini, non una modifica.
  **Fatti verificati oggi (2026-07-28).** Tre, e due cambiano il quadro. Primo: **`debinex` non esiste più nella configurazione** — zero occorrenze in `~/.claude.json`. Il finding «debinex 0 tool su 76, in 81 sessioni» è quindi obsoleto e non va più contato. Secondo: i connettori restano attivi **a livello di account, non di progetto**. Il campo `claudeAiMcpEverConnected` elenca Gmail, Google Drive, Google Calendar, Canva e Claude Code Remote; i server MCP locali in `~/.claude.json` sono due, `MiniMax` e `zai`; in `settings.json` risultano disabilitati `mcp-video` e `nuclear-arsenal`. I connettori Google vengono quindi caricati anche in sessioni che non c'entrano nulla, come quelle su questo proxy. Terzo: **il peso del blocco `tools` non è oggi misurato da nessuna parte.** I campi del sidecar `router-usage.jsonl` sono `ts, chat, orig, final, mode, client, status, input_tokens, output_tokens, cache_read, cache_creation`; [`src/router_debug.py:156`](../src/router_debug.py#L156) registra soltanto `has_tools`, un booleano.
  **Perché misurare prima di agire.** I 315M sono una stima di codeburn su euristiche proprie, **mai verificata sul nostro traffico**. Questo progetto ha già pagato due volte il prezzo di agire su numeri non verificati: la baseline delle 5.215 richieste si è rivelata un artefatto di misura, e i 502 del passthrough sono rimasti invisibili per giorni perché non finivano nel sidecar. Il router è nella posizione ideale per misurare davvero: vede il blocco `tools` di ogni richiesta, e lo ispeziona già in `src/tool_isolation.py` e `src/minimax_body.py`. Distribuzione attuale degli input su 117.805 richieste con `input_tokens` valorizzato: mediana 27.221 token, con un picco di 15.137 richieste nella fascia fra 14.000 e 16.000, che è il costo fisso tipico di una sessione.
  **Strumentazione: FATTA** (commit `066179a`). `collect_tools_stats()` in [`src/router_utils.py`](../src/router_utils.py), agganciata al logging centrale `streaming_relay.py:363` — quel file sta nella **ROOT** del repo, non in `src/`. Dietro `AIROUTER_TOOLS_TELEMETRY`, **default spento**: a flag spento la funzione esce prima di qualsiasi parsing e l'entry del sidecar resta identica, così i consumatori esistenti non vedono campi nuovi. 8 test nuovi, suite 120 → **128 passed**.
  **Misura eseguita (2026-07-28).** Attivata sul traffico reale con un drop-in systemd. Su una richiesta tipica di Claude Code: **64 tool per 142.056 byte**, pari a circa **56.822 token** stimati con lo stimatore del progetto ([`src/token_counter.py`](../src/token_counter.py), 2,5 byte/token sui modelli Anthropic nuovi — **non** la stima `char/4`, che era la causa del bug F9). Di questi, 40 tool sono MCP per 70.944 byte. Ripartizione per server: `claude_ai_Gmail` 32.379 byte ≈ 12.951 token (22,8% del blocco); `claude_ai_Google_Calendar` 26.378 byte ≈ 10.551 token (18,6%); `claude_ai_Google_Drive` 10.795 byte ≈ 4.318 token (7,6%); `zai` 1.351 byte ≈ 540 token (1,0%). I tool nativi non MCP sono 71.112 byte, il 50,1%. I tre connettori Google insieme valgono 69.552 byte, cioè **≈ 27.820 token, il 49,0%** del blocco `tools`.
  **Il dato che ridimensiona il finding.** Quei token sono **quasi sempre coperti dal prompt caching**. Su 9 richieste osservate, 7 hanno `input_tokens` pari a 2 e `cache_read` fra 116.037 e 244.049; solo 2 pagano il prompt pieno, e sono i cache miss. Il costo per richiesta è quindi di ≈ 27.820 token pieni **sul cache miss**, ma di ≈ 2.782 token equivalenti **sul cache hit**, dove `cache_read` costa un decimo dell'input.
  **Decisione che ne consegue.** La stima esterna di 315M token va considerata un **limite superiore teorico, non un risparmio disponibile**: presuppone che ogni richiesta paghi i tool a prezzo pieno, mentre il caching li copre quasi sempre. Resta reale il costo **non fatturabile**: i 27.820 token occupano spazio nella finestra di contesto di ogni richiesta, cache o no. Su un modello da 200.000 token come Haiku sono il **13,9% della finestra** speso in tool che l'audit indica come non usati (Gmail 1 tool su 16 in 702 sessioni, Calendar 0 su 11).
  **Tentativo di disattivazione via `disabledMcpServers`: NON FUNZIONA sui connettori claude.ai. Smentito dalla misura il 2026-07-29.** Il 29/07 alle 09:26 `~/.claude/settings.json` è stato modificato aggiungendo `disabledMcpServers: ["claude.ai Gmail", "claude.ai Google Calendar", "claude.ai Google Drive"]`, con i nomi esatti mostrati da `claude mcp list` (chiave diversa da `disabledMcpjsonServers`, che vale solo per i server da `.mcp.json`). La chiave è ancora lì, verificata rileggendo il file.
  **Misura che la smentisce.** Sidecar `router-usage.jsonl`, entry con `tools_mcp_servers` successive alle 09:27: **132 su 132 contengono ancora tutti e tre i connettori** (`zai` in 130). Ultima entry, `2026-07-29 17:47:01`: 58 tool, 137.243 byte, `{"claude_ai_Gmail": 32379, "claude_ai_Google_Calendar": 21571, "claude_ai_Google_Drive": 10795, "zai": 1351}`. Il calo di `tools_bytes` da 142.056 a 137.243 non è la rimozione dei connettori: sono −4.813 byte, quasi tutti dovuti a Calendar che è passato da 26.378 a 21.571 (schema del connettore cambiato). I 69.552 byte attesi non se ne sono andati.
  **Ipotesi «config non riletta» esclusa.** Il processo di questa sessione (PID 2304879, `claude` dell'estensione VSCode) è partito alle **17:41:47**, otto ore dopo l'mtime di `settings.json` (09:26:30). Ha letto la chiave e ha caricato i connettori lo stesso. Riscontro indipendente: i tool `mcp__claude_ai_Gmail__*`, `mcp__claude_ai_Google_Calendar__*` e `mcp__claude_ai_Google_Drive__*` sono presenti nel contesto della sessione avviata dopo la modifica.
  **Conclusione.** La prova statica dal bundle (`Nw(nome)` che ritornerebbe `{type:"disabled", tools:[]}` prima della discriminazione di tipo) è **contraddetta dal comportamento reale**: quel percorso non copre i connettori `claudeai-proxy`, oppure i loro tool entrano nel contesto per un'altra strada. **Ripiego certo, da fare a mano nel browser:** disconnettere i tre connettori da `https://claude.ai/customize/connectors`. La chiave in `settings.json` può restare (innocua) o essere rimossa; non serve a niente per questi server.
  **Cosa NON prova la disattivazione** (già verificato, non ripetere): `claude mcp remove` risponde «No MCP server named», conosce solo i server locali; `claude mcp logout` rimanda al browser perché riguarda le credenziali, non il caricamento; `claude mcp list` li mostra `✔ Connected` anche da disabilitati, perché fa un health check della configurazione; `claude -p` headless non carica MCP a prescindere (controprova con lista vuota: risultato identico).
  **Telemetria.** Resta accesa per raccogliere un campione più ampio — oggi il dato per-server viene da 320 entry ma da poche sessioni. Per spegnerla: cancellare `~/.config/systemd/user/ai-router.service.d/tools-telemetry.conf`, poi `systemctl --user daemon-reload` e riavviare con la procedura completa. Vincolo che resta valido: non eseguire `codeburn --apply` né `codeburn guard install`.

- [x] **400 Anthropic residui.** CHIUSA il 2026-08-04 con la seconda alternativa dell'expected outcome: il fix `a9d039c` li ha chiusi tutti.
  Verificato su logs/debug-errors.jsonl filtrando i soli path /v1/ (per escludere gli eventi sintetici dei test): dopo la mattina del 2026-07-26 restano DUE soli 400, e nessuno dei due appartiene alla classe di difetto descritta, cioe gli id server_tool_use non conformi.
  Il primo, 2026-07-27T23:02:48 in mode anthropic, e "prompt is too long: 208904 tokens > 200000 maximum": un limite di contesto legittimo dell'upstream, non un difetto del router. Curiosita utile: e esattamente l'errore che il 2026-08-04 e stato reso riconoscibile a providers.base._is_context_exceed_400, a cui mancava il marker "too long".
  Il secondo, 2026-08-01T10:44:43 in mode mix-am, ha client_model "health" e messaggio "invalid params, messages must not be empty": e una richiesta di health check con messages vuoto, non traffico utente.
  Non serve quindi strumentare la cattura del corpo del 400: la diagnosi e arrivata dai dati gia raccolti.

- [ ] **Guardia tool stranieri: da rilevazione a filtro.** La guardia response-side (`a85e505`) oggi rileva e non filtra, per scelta: riscrivere uno stream SSE romperebbe la numerazione dei `content_block`.
  **Sblocco:** un caso reale, `grep FOREIGN-TOOLUSE ~/.claude/logs/ai-router.log`. **Expected outcome:** finché il grep è vuoto, nessuna azione — un filtro su un fenomeno mai osservato non sarebbe verificabile.

- [ ] **(opzionale) Fascia peak GLM 14–18 Asia/Shanghai dal vivo.** La correttezza è già coperta in modo deterministico da `sviluppo/tests/test_peak_scheduler.py`: quattro bordi con ora iniettata, off-by-one catturato dalla prova del nove. Zero righe `peak-cap` nei log perché le modalità GLM sono poco usate.
  **Expected outcome:** nessuna garanzia aggiuntiva sulla correttezza; solo conferma su traffico reale.

- [ ] **Taratura dell'heartbeat sul traffico vero.** `CTX_GATE_HEARTBEAT_PCT = 0.30` (`src/ai-router-proxy.py:145`, env `AIROUTER_CTX_HEARTBEAT_PCT`).
  **Prossimo step:** se dopo una settimana il catalogo resta senza entry `ctx_gate`, abbassare la soglia. Costo nullo: il catalogo deduplica per (modalità, azione) e il throttle resta 300 s per chat.

- [ ] **(opzionale) Alzare `TRUNCATE_MAX_LEN` per usare più del 43% della finestra.** Emersa il 2026-08-02. Dopo la conversione del budget da token a caratteri, un body da 1135KB consegna all'esecutore 86.944 token su un `safe_limit` di 196.608, cioè il **43%**. Il resto è vincolato da `TRUNCATE_MAX_LEN = 1800` (`src/trim_smart.py`), il tetto per singolo messaggio dentro il riassunto — che è anche l'unità con cui `_smart_sample_middle` stima il costo quando applica il budget.
  **Perché non è stato fatto ora:** alzarlo cambia la qualità del riassunto (messaggi più lunghi ma meno numerosi, oppure riassunti oltre il budget) in modi che non sono stati misurati. Non è un difetto aperto: è un margine documentato.
  **Prossimo step:** misurare la qualità del riassunto a `TRUNCATE_MAX_LEN` 1800 / 3000 / 5000 su una conversazione reale lunga, verificando che (a) il budget resti rispettato e monotono — proprietà da cui dipende la convergenza dei loop di shrink nei tre percorsi — e (b) l'informazione critica di inizio conversazione sopravviva. **Expected outcome:** una soglia scelta con il dato a supporto, oppure la conferma motivata di 1800.

- [x] **Far girare il watcher del self-healing come servizio.** CHIUSA il 2026-08-04, commit 5bbaa57.
  La causa non era che nessuno lo lanzasse: il watcher NON POTEVA girare. Nel blocco if __name__ == "__main__" c'era un self-test che stampava "OK" e main() non veniva mai chiamata, quindi la CLI documentata (--once, --jsonl, --state, --interval) era irraggiungibile e i due file non venivano mai prodotti. Poiché router_policy.get_policy() e fail-open, l'assenza del file faceva ritornare {} e is_degraded() non degradava mai nulla: l'anello di self-healing era inerte per intero.
  Secondo difetto, trovato osservando il servizio dal vivo: follow() era una copia MONCA del ramo --once, senza emit_policy e senza la Fase 2.5 sulla telemetria m3-code, perció router-learnings.json si aggiornava ogni 60 secondi mentre router-policy.json restava congelato. Chiuso alla radice estraendo run_cycle, che i due rami ora condividono. Verificato dal vivo: entrambi i file si aggiornano allo stesso secondo.
  Servizio ai-router-self-healing.service come unità utente, abilitato, con copia versionata in sviluppo/systemd/. Nice=10 e IOSchedulingClass=idle per non togliere CPU al router; --interval 60. Il watcher processa 134.842 entry e produce 44 chiavi. Attivarlo NON cambia il routing: l'actuator nel proxy è solo osservatore, logga e non blocca il dispatch.
  I cinque casi sull'EWMA che vivevano nel self-test sono stati spostati in sviluppo/tests/test_watcher.py, più una guardia anti-regressione che fallisce se run_cycle smette di scrivere la policy. Totale 218 -> 223.

- [x] **Formalizzare i test di `router_policy` e `auto_fixer`.** CHIUSA il 2026-08-04, commit a774a2d.
  aggiunti sviluppo/tests/test_router_policy.py con 9 test e sviluppo/tests/test_auto_fixer.py con 14 test; il totale dalla root passa da 191 a 214, sopra i 168 fissati come traguardo. I test non sono tautologie: verificati con SETTE mutazioni indipendenti del codice sotto test, ognuna delle quali li fa fallire -- su router_policy soglia 0.5 a 0.9 (3 fail), >= che diventa > e cattura l'off-by-one sul bordo esatto (1 fail), fail-open sostituito da raise (2 fail); su auto_fixer _normalize che non normalizza le cifre (2 fail), min_count ignorato (1 fail), fallback per modalita rimosso (2 fail), taglio a cinque rimosso (1 fail). Sorgenti ripristinati bit-identici dopo ogni prova.
  BUG trovato scrivendo i test: il fallback di _suspected_files in auto_fixer era indicizzato per LINGUAGGIO (python, javascript, go...) mentre il campo mode contiene la MODALITA DEL ROUTER (mix-am 207, mixed 199, mix-gm 131, anthropic 101...), quindi non scattava mai e i ticket non indicavano alcun file sospetto; ora TYPICAL_FILES_BY_MODE mappa le modalita reali, con i tre nomi legacy come alias.

- [x] **Verificare il conteggio `text_blocks` su tutti e tre i provider.** CHIUSA il 2026-08-04, commit 5026027.
  il conteggio funziona su TUTTI i provider -- nella telemetria da 132.314 entry producono text_blocks>=1 anthropic 3316, minimax 541, glm 41, qwen 5. Il caso glm-5.2 + vision era un FALSO POSITIVO: quelle entry avevano thinking_blocks=1 e output_tokens 21.700, 87.770 e 15.628, e chiamare vuota una risposta da 87.770 token e falso. Il difetto stava a valle, in classify_outcome, che non riceveva affatto i thinking_blocks: il relay accumula solo i primi e gli ultimi 16KB, il content_block_start di tipo text cade nel buco centrale e non viene contato mentre il blocco thinking, che sta all'inizio, si. Non era cosmetico, perche empty e in FAIL_OUTCOMES e quelle entry alimentavano l'EWMA di fallimento fino a poter far marcare il modello degradato. Ora si distingue con stop_reason: vuoto significa stream mai concluso quindi "unknown" (misura incompleta), valorizzato significa fallimento reale quindi "empty". La discriminante regge sui dati: 153 entry su 153 con quella firma hanno stop_reason vuoto, contro il 13% delle entry sane. Impatto misurato rigiocando il classificatore su tutta la telemetria: 66 entry su 7731 passano da empty a unknown, nessuna altra transizione.
- [ ] **Osservare la salute del prompt caching nei prossimi giorni. Emersa il 2026-08-01.** (di sola osservazione)
  Dopo il fix dei due bug sui `cache_control`, la firma di salute è la riga `cache: OK bp=...` nel log del router con `input=2`. La patologia da riconoscere è `creation=0` con `input` a sei cifre, che significa prompt caching di nuovo morto. Se ricompare, ispezionare prima la coda dei messaggi e poi l'output di `filter_tools_for_backend`: il conteggio `bp=s2/m1/t0` dice subito quale sezione ha perso il breakpoint.
  **Expected outcome:** nessun intervento finché la firma resta sana; se ricompare la patologia, riaprire con il log a supporto.

- [ ] **Tenere monitorato il limite latente `sock_read=120` sugli stream. Emersa il 2026-08-01.** (di sola osservazione)
  Le richieste non-streaming sono passate a 600 secondi con il fix `7c84535`, mentre lo streaming eredita ancora i 120 secondi della sessione condivisa, dove quel valore protegge davvero dagli stall. Le 9 occorrenze storiche in modalità anthropic sono tutte `SSE=False` e precedenti al fix.
  **Expected outcome:** nessun intervento finché non compare un caso con `SSE=True` posteriore al fix.

- [ ] **Disconnettere dal browser i tre connettori Google. Emersa il 2026-07-29, riaperta dalla misura.**
  Mettere `claude.ai Gmail`, `claude.ai Google Calendar` e `claude.ai Google Drive` in `disabledMcpServers` dentro `~/.claude/settings.json` non li disattiva: la misura dinamica lo ha smentito, i tool continuano a essere caricati. I tre insieme pesano 69.552 byte, circa 27.820 token, il 49,0% del blocco tools. Il costo in fattura è però quasi tutto assorbito dal prompt caching, quindi l'urgenza è bassa: quello che resta è occupazione di finestra, il 13,9% su un modello da 200.000 token come Haiku. L'unica strada verificata è disconnetterli manualmente da `https://claude.ai/customize/connectors`.
  **Expected outcome:** dopo la disconnessione, `tools_mcp_servers` nel sidecar `router-usage.jsonl` elenca solo `zai`, e `tools_bytes` cala di circa 69.552 byte.

- [ ] **Misurare l'effetto delle tre leve sull'enforcement. Applicate il 2026-08-03, effetto da verificare fra qualche giorno.** Diagnosi: in `mix-am` Anthropic consuma **9,3×** MiniMax sull'input equivalente (299,0M contro 32,1M, cache pesata al 10%) mentre l'output è rovesciato (MiniMax 13,0M contro 7,65M). Il 38,6% delle richieste Anthropic era `coding` (3.018 contro 168) e l'enforcement bloccava il 3% delle scritture (2.202 allowed contro 70 denied). Leve applicate a `~/.claude/hooks/enforce_hierarchy.py`: estensioni web/shell/config in `CODE_EXTENSIONS` (`.html` da solo valeva 657 eventi), soglia micro-edit 15→5 nelle mode deleganti, `.md` come codice solo dove si delega con deny verso `m3-wiki`. Test `~/.claude/scripts/test_enforce_hierarchy_leve.py` 16/16.
  **Expected outcome:** su una nuova finestra di 7 giorni, la quota `task_class=coding` su Anthropic scende sotto il 38,6% e le `denied` in `hierarchy-violations.jsonl` salgono ben sopra le 70. Attenzione: la quota **strutturale** (~226k token di contesto per turno del main contro 61k dell'esecutore) **non** è aggredibile per questa via — se il rapporto non migliora abbastanza, la leva successiva è ridurre i round-trip del main, non aumentare le deleghe.

## Chiusi — indice per fase

Una riga per fase; il dettaglio sta nei commit e nel checkpoint consolidato.

- **19 lug — stabilizzazione.** Crash-loop `UnboundLocalError` (`80b6ab5`), root cause dei 404 MiniMax = header `Host` inoltrato (`a5c31af`), isolamento tool centralizzato (`462f181`), retry-storm mix-am con THINK non bloccante (`c3a2ca8`), 3 bug della modalità GLM pura (`6e51322`), sistema di debug centralizzato.
- **21–22 lug — lavori lunghi e modalità miste.** TRIM INTERCEPT rimosso (`71497ae`), THINK sintetico su Haiku con VERIFY a campione (`d2bb6aa`), streaming passthrough sulle miste (`cd6b4ef`, `5f6c9f5`), retry 429 certificato (`e6fb4fb`, `e5dc339`, `582eca2`), esecutore non più cieco a system e immagini (`bb84a41`, `447d1e6`), warning spostati nell'header `x-ai-verify` (`3b5a664`), isolamento web search (`a227ea3`).
- **23–25 lug — refactor.** Merge totale con WIP worktree scartato come superseded, pipeline morte e codice morto rimossi (`2bd4212`, `0174894`), refactor tunnel con i selettori di modello Anthropic fuori dal router (`99dcc0d`), `VERSION_MANIFEST` riallineato 7/7, `version_drift_check.py` riscritto da stub.
- **26 lug — guardrail e igiene.** Guardrail di contesto G1/G2/G3/G5/G6 (`c58fa1e`, `ada9af7`, `1031dde`, `10de32c`), switch di modalità a voce rimosso, `inverse`/`interactive` eliminati ovunque, 86 override per-chat azzerati, manuali riscritti sui 6 modi reali (`4759195`), id `server_tool_use` sanificati (`a9d039c`), guardia response-side sull'isolamento tool (`a85e505`), mix-gm smentito con misura.
- **AQ Backlog — chiuso.** AQ-RL1/RL2/FIX1 (`8e40532`), AQ-REF1 (`e8fc50c`), AQ-REF2 (`3a64731`), AQ-REF3/4/5 (`0280326`, poi `src/pipelines/` eliminata dal refactor tunnel), AQ-REF6 (limiter già indipendenti), AQ-REF7 (`70dc5e2`), AQ-TEST (`1e40859`).
- **4 ago (notte) — loop self-healing CHIUSO e reso attivo.**
- **4 ago (notte fonda) — self-fixer L2 ATTIVO.** Il sistema ora si autoripara: `src/self_healing/fixer.py` (commit `57ae5ef`) porta il ticket ricorrente in worktree isolato, l'agente AI propone la patch, 4 gate la verificano (zona lecita, import reale, ruff, suite), poi merge + restart sicuro + healthcheck 8 porte; post-merge fallito = revert + reset-failed + verifica, fail-open ovunque. Zona proibita: self_healing/, router_policy.py, unit. Timer 6h. Prima corsa autonoma: 09:31. Generato da qwen-code, 7 difetti corretti in revisione, 13 test con prove di mutazione, suite 257 passed. Il chain fallback promesso dal commento dell'actuator non esisteva: nessun consumatore di `router-policy.json` in tutto `~/.claude`. Ora `get_chain()` in `~/.claude/lib/router_chain_dispatcher.py` devia l'ESECUTORE degradato sul primo fallback sano (mai il THINK, fail-open se tutti degradati), la catena qwen entra nel dispatcher (7/7 modalità), `emit_policy` emette `*` invece di `?` (`5545b4a`) e la telemetria qwen-code porta `finish` anche sui percorsi di errore. Prove: matrice deviazione 5/5 con policy sintetiche, 235 passed, deviazione LIVE `minimax-m2.7-hs -> claude-sonnet-5` su mix-am con la policy reale.

## Note di manutenzione

- `PROJECT-TOD.md` è superato da questo file: il suo backlog AQ risulta chiuso e la sezione «Done» duplica commit già consolidati qui.
- `.claude/PROMPT_CONTINUA_20260728.md` è stato **eliminato il 2026-08-03**: puntava a HEAD `847d3b4` e al task dei 4 `fixture 'h' not found`, chiuso il 28/07 con `41a4ef8`. Come i precedenti, la storia resta in git. Il punto di ripresa è ora questo file più `.claude/WIKI.md`.
- **`.claude/WIKI.md`** è la narrazione consolidata del periodo 27/07 → 03/08: architettura, otto campagne con i 49 commit, lezioni di metodo, divieti operativi e comandi di verifica. Questo TODO resta la lista operativa; il wiki è il contesto.
- I 97 checkpoint di sessione in `.claude/checkpoints/` sono sostituiti da `CP_20260727_1600.md` e archiviati in `archivio-checkpoint-20260623-20260726.tar.gz`. I 10 checkpoint successivi (27/07 → 02/08) restano al loro posto e sono riassunti in `WIKI.md`. La cartella è in `.gitignore:17`, quindi vive solo sul filesystem.
